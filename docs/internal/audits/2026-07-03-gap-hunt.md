# Rakkr — Gap Hunt Findings (per-channel room ownership)

Adversarial correctness/security audit of the Rakkr monorepo, run as the
repeatable loop in [`docs/contributing/audit-workflow.md`](../../contributing/audit-workflow.md):
independent read-only fan-out hunters plus an adversarial verify pass on every
lead, then red→green proof for each confirmed fix. This audit opened right after
PR #23 (**per-channel room ownership**) landed on `main`, so that fresh surface is
the primary target. Target: **5 consecutive clean runs.**

| | |
| --- | --- |
| **Branch** | `claude/admiring-hawking-8e57db` (Runs 1–24; merged to `main` via PR #29) → **resumed on `claude/vigorous-mcnulty-d92473`** (Run 25+) |
| **Base** | Runs 1–24 rebased onto `923397f0`; **Run 25 resumed off merged `main` `25873566`** (PR #29 brought the whole prior audit + the 16-item backlog batch into `main`) |
| **Started** | 2026-07-03 |
| **Completed** | _5-clean loop paused post-Run-40 at operator direction (2026-07-04). Follow-on operator-directed work (2026-07-04): a **catalogued-backlog sweep** landed 21 of the 23 open catalogued/suspected/coverage items (commits `BG`–`BN`), each red→green + full-gate verified. The 2 remaining are recorder-agent Rust items (`R40-ENHANCE-TAIL`, `R37-CONCURRENT-STATE`), deliberately deferred to Linux/audio-rig validation — recipes are ready in their rows below._ |
| **Runs** | 40 so far — **streak 0** (Run 39 was clean → streak 1, but Run 40's fresh passes found a metrics-escaping gap (`BF`, fixed) + a confirmed enhanced-rendition tail-clip (`R40-ENHANCE-TAIL`, catalogued — safe fix needs rig/audio verification) → dirty, streak reset to 0) |
| **Findings closed** | 95 (34 in the run loop + 16 clearing the catalogued/coverage backlog + 4 in Run 25 + 3 in Run 26 + 1 in Run 27 + 4 in Run 28 + 2 in Run 29 + 1 in Run 30 + 1 in Run 31 + 1 in Run 32 + 2 in Run 33 + 1 in Run 35 + 1 in Run 37 + 1 in Run 38 + 1 operator-directed (`BE`) + 1 in Run 40 + **21 in the operator-directed catalogued-backlog sweep `BG`–`BN`**; each with a red→green proof, except the docs fixes `Q` and `BA`) |
| **Result** | _run loop converged at streak 1 of 5 (high-water mark 2, Runs 9–10); the catalogued backlog was then landed (16 items). Resumed on a fresh worktree after PR #29 merged everything to `main`: **Run 25 audited the merged 16-item batch as landed code** and found 4 new bugs (2 High, 1 Medium, 1 Low) + a flaky-gate fix — dirty, streak reset to 0. A separate user-requested pass also **split every production-source + Rust file >900 LOC** into cohesive modules (pure refactors). Finally, an operator-directed **catalogued-backlog sweep** (`BG`–`BN`) drained the "Catalogued / suspected" table from 23 open items to 2: the TypeScript/deploy items (upload dedup+finalize+cache-gating, transactional access writes + OIDC conflict handling, DB-aware `/readyz` + ordered migration Job + helm render gate, admin health-count scoping, duplicate-create 409s, roster-aware SSE gate, web error boundary, streaming-upload proxy, safe SPA caching, orphan-Postgres guard, DST-capture documentation, calendar-chip TZ) are all resolved; only the 2 recorder-agent Rust items remain, deferred to the rig._ |
| **Gates at close of Run 40** | green — API 577 pass / 13 skip (non-DB) + DB-gated 24 pass, web 138, tsc, oxlint, oxfmt, LOC (every production-source + Rust file ≤900), cargo test + clippy + rustfmt, and the rbac / scheduler / storage / operations / watchdog / oidc / settings / switcher / recordings / generic-device / first-reliable baseline verifiers. Run 40 landed `BF` (+1 metrics test → 577); tsc/oxlint/oxfmt/LOC re-run green, `main` current. (`R40-ENHANCE-TAIL` is a Rust DSP finding whose safe fix + red→green needs Linux/engine + real-audio verification — catalogued, not landed on this Windows env.) |
| **Gates after sweep `BG`–`BN`** | green — every one of `BG`–`BN` was committed only after its own slice went red→green and the full gate passed (tsc, oxlint, oxfmt, `check:loc`, API + web suites, `db:verify` replay against local Postgres, the new `helm:check` render gate from `BN`, and the baseline verifiers). Re-verified now on the merged branch: API **608 pass / 19 skip / 0 fail** (non-DB; the 19 skips are the DB-gated suite — incl. the sweep's new `auth-access-tx` + `oidc-race-conflict` DB tests — run green against local Postgres during the sweep), web **140 pass**, `check:loc` clean (ledger 842 LOC). Only this markdown ledger changed after `BN`, so the code-level gate legs hold from `BN`'s green run. The 2 open Rust items (`R40-ENHANCE-TAIL`, `R37-CONCURRENT-STATE`) are unchanged/deferred to the rig. |

**Status legend:** `FIXED` = failing test + fix landed on this branch · `RESOLVED`
= closed indirectly by another fix · `CATALOGUED` = confirmed/reproduced, fix
recommended but not applied (blast radius, product call, or hardware verification
needed) · `SUSPECTED` = strong lead, not fully substantiated · `COVERAGE` = no bug,
a missing-test gap.

PR #23's room core is largely sound — the room-resolution helpers
(`channelRoomId` / `nodeRoomIds` / `resolveSelectionRoom`), the per-level scope-target
expansion (`addNodeScopeTargets` union vs `addNodeResourceTargets` single-room), the
meter per-channel filter (`filterMeterFrameForUser`), and the ad-hoc recording and
schedule **create** paths all correctly enforce the shared-node isolation invariant.
The confirmed gaps clustered where a request or a later event **changes the room a
resource resolves to** — schedule update, channel reassignment, scheduled-run time —
and one authorization skip on the room-less edge.

---

## Everything fixed

23 findings. Tags: `A`–`E` (Run 1), `F`–`H` + `R2-*` (Run 2), `J`–`L` (Run 3), `M`
(Run 4), `N` (Run 5), `O`–`Q` (Run 6), `R` (Run 7), `S` (Run 8), `U` (Run 11), `V`–`W`
(Run 12), `X`–`Y` (Run 13), `Z` (Run 14), `AA` (Run 15), `AB` (Run 16), `AC` (Run 17), `AD` (Run 18), `AE` (Run 19), `AF`–`AG` (Run 21), `AH` (Run 23), `AI`–`AL` (Run 25), `AM`–`AO` (Run 26, all adversary-on-Run-25), `AP` (Run 27), `AQ`–`AT` (Run 28), `AU`–`AV` (Run 29), `AW` (Run 30), `AX` (Run 31), `AY` (Run 32), `AZ`–`BA` (Run 33), `BB` (Run 35), `BC` (Run 37), `BD` (Run 38), `BE` (operator-directed, post-Run-38), `BF` (Run 40). Each run adversarially audits the prior run's own
fixes — `F` (Run 2) is a hole Run 1's Fix A left; `K` (Run 3) is a residual in Run 2's
Fix G3; `N` (Run 5) reopened `M`'s leak via a different auth path; `O`/`P` (Run 6) are the
SAME "node-scope mistaken for full-node authority" class as `N`, in `scopedRooms` and
`/metrics`. The listen/meters isolation seam (`M`→`N`→`O`/`P`) was the audit's deepest.
Runs 11–13 shifted to fresh dimensions outside the room-ownership focus (recording
retention, settings-create validation + web picker limits, then secrets-atomicity +
switcher transport), each surfacing new bugs — the diverse-rotation phase.

### Critical

| ID | Run | Area | Fix | Commit |
| --- | --- | --- | --- | --- |
| AD | 18 | node-secrets/atomicity | The twin of `X` on the node API **bearer** credentials (found by the atomicity sweep). `PostgresNodeStore.rotateCredential` revoked the prior credential(s) then inserted the new one in two separate statements with no transaction and no DB-level guard, so (a) a failed insert after the revoke left the node with ZERO active credentials — it could no longer authenticate (node lockout), and (b) two concurrent rotations could each insert an active row (an un-revoked, still-valid stale token). Wrapped revoke+insert in a transaction (`createCredential` accepts the tx) + added partial unique index `node_credentials_active_node_idx WHERE revoked_at IS NULL` (migration `0042`); a failed/racing rotation rolls back, leaving exactly one active credential. Red→green via a DB-gated 16-way rotation-race test. (Pure `updated*` transforms extracted to `node-store-updates.ts` for the LOC guard.) | `e409732c` |
| U | 11 | recording/retention | Recording-job **retry** (`purgeRetriedRecordingChunks`) deleted the failed attempt's chunk rows/files + reset the recording but left its `upload_queue_items` orphaned (no FK cascade). A stale `failed` item pinned the recording in an unsettled-upload state so retention NEVER reclaimed its cache; a stale `queued`/`retrying` item made the upload runner re-attempt a purged file → repeated `cache_path_missing` health events. Now also calls `deleteUploadQueueItemsForRecording` (mirrors the delete path), covering the single + bulk retry routes. | `e22ffb47` |
| S | 8 | oidc/auth | PR #25's `oidcGroupsFromClaims` capped the derived group id (slug, 120) but set the display `name` to the raw IdP claim value uncapped, while `access_groups.name` is `varchar(160)`. An IdP group claim > 160 chars (e.g. a full group DN) trips a Postgres length constraint on upsert → auth-service `markDatabaseUnavailable` PERMANENTLY latches `dbAvailable=false` → all later auth silently falls back to the memory store until restart (login DoS + auth degradation). Now capped to 160, matching the operator create schema + column. | `512cf440` |
| R | 7 | health/scope | `resource-scope-targets.ts` expanded a health event with a `scheduleId` but no `recordingId` via the node's ROOM UNION (treating "no recording" as "node-level"), so a room-A operator saw room-B's schedule health events (busy/cross-room, with `scheduleName`/`busyChannels` details) on a shared node. Now only a genuinely node-level event (no recording AND no schedule) takes the union; a schedule-scoped event follows its schedule's single room. The 4th (and structural, scope-target-level) instance of the room-union class. | `eb7ac6eb` |
| P | 6 | metrics/authz | The `/metrics` scrape path fed WHOLE-node meter frames + listen-monitor chunks to the Prometheus renderer with no per-room filter (its own `scopedNodes` admitted shared nodes via `hasResourceScope(node)`), so a room-A grant/policy holder scraping `/metrics` got room-B's per-channel gauges (rms/peak/speech/snr) + monitor-chunk metrics. Now `scopedMeterFrames` filters each frame via `filterMeterFrame` and `scopedListenMonitorChunks` gates on `canServeWholeNodeMonitor`. | `d1fc5c98` |
| N | 5 | listen+meters/authz | `meterRoomAccess` returned `"all"` whenever `hasResourceScope(user, {node})` — but a node target expands to the room UNION, so a caller with only a room-A resource grant or a room-A access-policy allow on a shared node got whole-node monitor audio (bypassing Fix M) AND unfiltered meters (pre-existing). Now `"all"` requires genuine full-node authority (`hasFullNodeAuthority`: owner/admin or node/site/wildcard grant/policy, no room expansion); the per-room path resolves to exactly the caller's owned rooms. | `0b2e711c` |
| B | 1 | schedule/node room reconcile | `PUT /nodes/:id/channel-rooms` mutated channels but left `schedule.roomId` stale → wrong `scopedSchedules` visibility, stale RBAC edit-gate, stale calendar roster → reassignment now re-homes every affected schedule + roster (cross-room selection ⇒ room-less) | `87d370d7` |

### High

| ID | Run | Area | Fix | Commit |
| --- | --- | --- | --- | --- |
| BE | (post-38) | auth/oidc-security | **Operator-directed fix of the CONFIRMED `R38-OIDC-EMAIL-LINK`.** OIDC linking keyed on **email** (`email ?? preferred_username ?? upn`, no `email_verified`; the stable subject `oid`/`sub` was captured but unused), so a token whose email matched an existing account **merged onto it** — and since the built-in local-admin row resolves to **owner** via a fallback, a federated login presenting the admin's email inherited owner (account takeover). `users.email` is `unique`, which is exactly why the old code merged rather than duplicated. Fix (migration `0044` + auth core): added `users.external_id` (unique index; null for local users) and link OIDC logins on the **IdP subject**; on a subject miss, adopt a legacy email-linked OIDC row (backfill its subject) but **refuse** (`oidc_email_conflict` → 403) when the email belongs to any other identity (local/admin or a different subject) — never merge; and reject an explicitly `email_verified:false` claim. Red→green via `oidc-user-linking.test.ts` (memory subject-link + collision-reject, a DB-gated takeover test proving an OIDC login can't assume a local account by email, subject re-link across email change, legacy-row adoption, + the `email_verified` unit reject — all 6 fail on the pre-fix email-merge). `db:verify` replays `0044`; OIDC baseline doc + verifier updated. | `2e9a7c4e` |
| BA | 33 | deploy/helm-secrets | The documented `externalSecrets`/`sealed` recipe (`docs/operations/deployment.md`) supplied `DATABASE_URL` via an external secret but left the **bundled Postgres enabled** — whose `Secret` renders `POSTGRES_PASSWORD` from the empty `postgres.auth.password` (independent of the app secret's `DATABASE_URL`). So Postgres booted trusting an EMPTY password while the API presented the operator's real one → every connection fails `password authentication failed`, and the whole controller never starts, on the exact recipe the docs tell operators to copy. Found by a hunter running `helm template` live across all three backends. Fixed the recipe to `postgres.enabled=false` (external DB is the natural pairing) and documented the bundled-Postgres coordination requirement (`postgres.auth.existingSecret`/matching `password`) in both `deployment.md` and `values.yaml`. Docs/config fix (no committed red→green — verified by `helm template`: the corrected recipe renders no bundled Postgres secret). Adjacent gate gap catalogued as `R33-HELM-RENDER-GATE`. | `58f87a2b` |
| AZ | 33 | deploy/k8s-availability | The API `Deployment` declared no `strategy`, so it inherited the default **RollingUpdate** — but the API mounts a single **ReadWriteOnce** PVC (`api-pvc.yaml`; `values.yaml` `api.persistence.accessModes: [ReadWriteOnce]`, `replicaCount: 1`) for its JSON stores + recording cache. On any image-tag `helm upgrade`, RollingUpdate starts the NEW pod before terminating the old one; both mount the same RWO volume, so the new pod is stuck `Multi-Attach` and — with `maxUnavailable` rounding to 0 at `replicas:1` — the rollout **deadlocks indefinitely** (a stuck upgrade, not a clean cutover). Set `strategy: { type: Recreate }` — the correct single-writer posture (tear the old pod down first, brief cutover, never double-mount). Invisible to the Compose smoke (no rolling-update semantics) and to a fresh `helm install` (only an *upgrade* deadlocks). Red→green via a manifest-invariant test (`deploy-helm-manifests.test.ts`); `helm template` confirms `strategy: type: Recreate` renders. | `58f87a2b` |
| AY | 32 | deploy/k8s-availability | The web pod is nginx (static console + `/api` proxy), but its Helm **liveness AND readiness** probes both hit `/healthz` — which nginx `proxy_pass`es to the API (`deploy/nginx/default.conf.template`), so the web tier's own lifecycle was gated on API availability. **Liveness:** an API outage of ~60s (`failureThreshold 3 × periodSeconds 20`) makes Kubernetes kill + restart the perfectly-healthy web pods → crash-loop storm during any API incident (and a slow first-install migration can trip it mid-rollout). **Readiness:** a transient API blip pulls every web pod out of the Service endpoints, and since the ingress routes ALL traffic (incl. `/api`) only to `-web`, that makes the API unreachable through the ingress exactly as it recovers. Liveness reflecting a *dependency* is a textbook k8s anti-pattern. Added a self-contained `location = /nginx-health` that returns `200` locally without proxying, and pointed both web probes (+ the Compose web healthcheck) at it, so the web tier stays live/ready whenever nginx can serve. Invisible to the Compose smoke (its healthcheck is a readiness-style `depends_on` gate that never kills the container, and the API is always up during the smoke). Red→green via a config-invariant test (`deploy-web-health-probe.test.ts`) asserting each web probe targets an nginx-local location with no `proxy_pass`. | `fcf6e807` |
| BC | 37 | schedule/calendar correctness | Fresh scheduler pass: `move-occurrence` recomputed the skipped series' `nextRunAt` via `nextRunAtForRecurrence(skippedRecurrence, tz, undefined)` — `after` defaulted to `now` and NO parity anchor was passed, so for an `interval>1` recurrence (bi-weekly / every-N-days / every-N-months) the every-N parity was re-derived from the CURRENT week instead of the schedule's persisted phase. Moving one occurrence therefore silently shifted EVERY future occurrence to the wrong week/day/month whenever `now` fell in the off-parity window — intermittent (≈half the time) and evidence-invisible (the audit says `succeeded`). This is the `AT` parity class on the move-occurrence WRITE path, which `AT` (calendar READ path) never covered; every prior move-occurrence test used `interval:1` where parity is a no-op. Fixed by passing the schedule's phase (`before.nextRunAt`) as the `parityAnchor` (6th arg) — parity measured from the true phase while the scan still starts at `now`, so a later-in-series move doesn't skip the still-upcoming immediate occurrence (verified `parityAnchor` is correct where `previousRunAt` would wrongly advance past it). Red→green via a route test pinning `now` to the off-parity week with `mock.timers` (pre-fix `2026-07-06`, post-fix `2026-07-13`). | `4bee2315` |
| AX | 31 | deploy/upload-pipeline | The web tier's nginx (`deploy/nginx/default.conf.template`) and the Helm ingress (`values.yaml` `ingress.annotations`) set NO body-size limit, so both fell back to nginx's **1 MB compiled default** — but the recorder agent PUTs recording renditions to `/api/v1/recordings/:id/cache-file` up to `RAKKR_RECORDING_CACHE_MAX_BYTES` (4 GiB default), and in every shipped topology that traffic traverses the web/ingress nginx (the Helm ingress routes ONLY to the `-web` service; `docs/operations/deployment.md` documents ingress → web → API). So a recorder node pointed at the production host got `413 Request Entity Too Large` on essentially every cache-file upload — the whole capture→upload→object-storage pipeline was broken in a standard reverse-proxied/Helm deploy, while the Compose + fake-controller smokes never caught it because they hit the API on `:8787` **directly**, bypassing nginx entirely (`client_max_body_size` appeared nowhere in the repo). Set `client_max_body_size 0` on the `/api/` location and shipped the `nginx.ingress.kubernetes.io/proxy-body-size: "0"` annotation as a values default; both defer to the controller's authoritative bounded cap (`agent-cache-upload-body.ts`, which safely aborts even a chunked/lying-Content-Length body), and the behavior is documented in `deployment.md`. Red→green via a config-invariant test (`deploy-upload-body-size.test.ts`) that reads both proxy configs and asserts each admits a body at least as large as the API's `recordingCacheUploadMaxBytes()`. | `c02ce839` |
| AT | 28 | schedule/calendar correctness | Closes the confirmed `R28-CALENDAR-PARITY`. `windowScheduleOccurrences` seeded the scan with no `previousRunAt`, so for `interval>1` (bi-weekly, every-3-days, every-2-months) the every-N parity was re-derived from the WINDOW EDGE — the calendar rendered the opposite fortnight / wrong days and disagreed with the correct schedule-detail preview. Decoupled the parity anchor from the scan cursor: `nextRunAtForRecurrence` + `nextDaily/Weekly/MonthlyRunAt` take an optional `parityAnchor`; the window scan passes the schedule's true `nextRunAt`, so parity is measured from the schedule's phase while the scan still starts at the window edge (occurrences before the anchor still enumerate correctly). The param defaults to prior behavior, so `advanceScheduleAfterRun` and every other caller is byte-identical. Red→green (bi-weekly window on the opposite fortnight); full schedule-engine + scheduler baseline unchanged. | `cd3a638a` |
| AR | 28 | upload/data-loss | Broad convergence: the upload runner's `deleteCacheAfterUpload` path deleted EVERY rendition — including the raw master (`rawCachePath`) — but the raw rendition is **supplementary and never enqueued for external upload** (only the enhanced/primary is), so a confirmed primary upload silently destroyed the raw master documented as the source of truth (`keepRaw` was never consulted). `deleteRecordingCacheFile`/`deleteRecordingChunkCacheFile` gained an `includeRaw` option (default true — byte-pressure retention still reclaims raw); the upload runner now passes `includeRaw: false`. Red→green (raw survives a `deleteCacheAfterUpload` run; the uploaded primary is still reclaimed). | `2e2b666c` |
| AM | 26 | recorder-agent/availability | Adversary-on-`AJ`: `AJ` made the health-event append best-effort, but the SAME heartbeat tick also runs `run_idle_recorder_cache_sweep` when idle, whose manifest load/save I/O (`run_recorder_cache_sweep`) still `?`-propagated a local write failure (full disk / unwritable manifest dir) out of `main()` — the exact daemon-crash `AJ` set out to prevent, via a second local-evidence write on the same tick. Swallow-and-log the sweep error and skip the tick (mirroring the health-append guard). Rust red→green (manifest path pointed at a directory so the read fails). | `b4111d6b` |
| AI | 25 | auth/availability | Auditing the merged 16-item batch: `R8-DBLATCH` (Run 19 catalogue, landed in the batch) made `markDatabaseUnavailable` **re-throw** on a Postgres constraint/data error (SQLSTATE class 22/23) instead of latching — but that helper is invoked from ~10 **fire-and-forget persistence + read/authorization** callers that must never throw. A valid-credential login carrying an `X-Forwarded-For` longer than `auth_sessions.ip_address varchar(120)` tripped `22001` on the session INSERT → the re-throw unwound out of `login()` → the route returned `401 Invalid credentials` (a legitimate user behind a long proxy chain locked out), and left an orphaned in-memory session. Scoped the re-throw OUT: on a constraint error `markDatabaseUnavailable` now logs + returns (no latch — preserving R8's intent — and no throw), so fire-and-forget/read callers degrade gracefully and the access-write callers still surface their own `AuthError`. Red→green via a DB-gated login-with-over-long-forwarded-for test. | `852a5328` |
| AJ | 25 | recorder-agent/availability | `R14-HEALTH-FATAL` (landed in the batch) only made `update_meter_health`/`update_system_health` best-effort via `apply_tick_health_updates`; the heartbeat/recovery tick's other ~9 `append_and_sync_health_event(...).await.context(...)?` sites (heartbeat/meter/node-config/capture/monitor sync edges + idle cache sweep) still `?`-propagated a **local** health-log append failure out of `main()` — the exact daemon-crash the fix set out to prevent (full disk + a controller-connection flap → systemd crash-loop, worst on the degraded capture-failed path). Made the local append best-effort **at the source** in `append_and_sync_health_event` (log + return `Ok`, mirroring its already-best-effort controller-sync half), closing every call site at once. Red→green (Rust, unwritable-sink). | `53505822` |
| AH | 23 | db-schema/data-loss | `recording_chunks.size_bytes` was a 32-bit signed `integer` (max ~2.1 GB), but a single chunk of multichannel/uncompressed audio — e.g. a 32-channel X32 WAV recording (Rakkr's primary rig) — exceeds that in minutes, and the chunk byteLength is assigned raw. The upsert threw `value out of range for type integer` and the chunk failed to persist (breaking cache-utilization + retention metadata). Widened the column to `bigint` (JS number, exact to 2^53 = 8 PiB; migration `0043`); the shared schema already accepted unbounded `sizeBytes`. Red→green via a DB-gated direct-insert of a `>2^31` size (out-of-range without the migration, round-trips with it). | `e97ba15c` |
| AF | 21 | agent-contract/data-loss | `recorderCacheRetentionFromValue` (`recording-job-command.ts`) deserialized a persisted recording job's recorder-cache retention to only `{deleteAfterUpload, policyId}`, silently dropping the `maxAgeDays`/`maxBytes`/`minFreeDiskPercent` thresholds the shared schema defines and the agent needs for deferred cleanup. On job reload (`nextRecordingJob`) the agent received an incomplete policy and never ran age/bytes/free-disk cache sweeps → the recorder-node cache grows unbounded (eventual disk-full on the node). Now preserves all three nullable thresholds. Red→green on `commandFromValue`. | `f634c043` |
| AE | 19 | node metadata/atomicity | `heartbeat()` and `update()` each did a read-then-write of the node `metadata` JSONB (SELECT, merge in app, UPDATE) with no row lock. A frequent agent heartbeat concurrent with an operator `update()` races: the later writer, merging from a stale read, clobbers the other's metadata changes (last-writer-wins) — e.g. an operator's `recordingCapacity`/`audioDefaults` edit silently reverted by a heartbeat that read the pre-update row. Wrapped each read-modify-write in a transaction with `SELECT … FOR UPDATE` so the two serialize per node; both changes survive. Red→green via a DB-gated test (operator capacity update vs 16 concurrent heartbeats: reverted without the lock, preserved with it). | `75ce321b` |
| A | 1 | schedule/authz | `PATCH /schedules/:id` re-derived `roomId` from a new channel selection but never re-authorized against the new room → a room-A booker could seize room-B's channels on a shared node → re-check via the roster-aware `authorizeTarget` (mirrors ad-hoc recording start); `authorizeTarget` wired into schedule routes | `87d370d7` |
| C | 1 | scheduler/attribution | `materializeScheduledRecording` used `schedule.roomId` verbatim → a scheduled run attributed the recording to the stale snapshot after a channel reassignment → `queueScheduledRecordings` now re-resolves the room live from the node and defers (fail-closed) a now-cross-room occurrence | `87d370d7` |
| F | 2 | schedule/authz | Fix `A` re-authorized only when the new room was *set*, so a room-A booker could repoint a schedule onto UNOWNED (room-less) channels — the check short-circuited on `updateRoom.roomId &&`. Now falls back to role/grant node authority when the resolved room is undefined (mirrors Fix `D`) | `3b5e0eb5` |
| J | 3 | web/hooks | `room-detail.tsx` called `useDocumentTitle` AFTER an early return, so the auth pending→resolved transition changed the hook count and crashed the room detail page to the error boundary on cold load → hoisted the hook above the early return, and enabled `react-hooks/rules-of-hooks` in oxlint as a permanent repo-wide guard | `4146211c` |
| O | 6 | room-list/authz | `scopedRooms` added EVERY room of a node to a caller's visible-room list whenever `hasResourceScope(user,{node})` — true for a room-A grant/policy via room expansion — so a room-A-only principal saw sibling room B (name/location/notes) in `GET /rooms`. Now uses `hasFullNodeAuthority` for the all-rooms branch + adds only rooms the caller holds direct room authority on. (Recordings stayed safe — `scopedRecordings` filters by the recording's own room.) | `d1fc5c98` |
| M | 4 | listen/authz | `POST /nodes/:id/listen` + `GET .../listen/stream` served the whole-node monitor WAV gated only at node granularity, so a room-A roster `listen` operator heard room-B's live audio on a shared node (the `/meters` route filters per-channel; listen had no analogue). A pre-mixed WAV can't be filtered post-hoc, so both routes now 403 unless the caller owns every channel's room (`canServeWholeNodeMonitor`). Flagged by an earlier audit but never landed. | `9e865b06` |
| G1 | 2 | room/delete | `JsonRoomStore.delete` had no FK guard, so a schedule-referenced room deleted with 204 (Postgres RESTRICT → 409) and stranded a dangling `schedule.roomId` → enforce the `schedules.roomId` reference in the route for BOTH backends (409 `room_in_use`) | `3b5e0eb5` |
| G2 | 2 | room/roster | Room delete never cleaned the roster outside the Postgres FK cascade → orphaned grants + leak on slug reuse in JSON mode → added `RoomRosterStore.removeForRoom` (both backends) + call it on delete | `3b5e0eb5` |

### Medium

| ID | Run | Area | Fix | Commit |
| --- | --- | --- | --- | --- |
| BB | 35 | web/wrong-value | Fresh web-console runtime pass: the health page's summary tiles (Active Critical / Open / Muted / Resolved) counted only the current paginated page (`healthEventSummary(events)` where `events` is one 50-item page), so with more than a page of matching events they **undercounted** and visibly contradicted the true total shown in the header line right above them — on the primary incident-triage screen an operator scanning "Active Critical" got a page-local count, not the real number of live fires. The recording-jobs workbench already solved this (server `recordingJobStatusSummary` over the full filtered set, G74); health had kept its pre-pagination client counter. Mirrored the jobs pattern: a shared `HealthEventStatusSummary` + an API `healthEventStatusSummary()` returned as `{ data, meta, summary }` (unrestricted branch summarizes unscoped to match its unscoped `count` total; restricted branch reuses the already-loaded full visible set), and the console prefers the server summary. Red→green via an API test asserting the summary reflects all matches across both reader branches while the page stays limited. `R35-ERROR-BOUNDARY` catalogued alongside (no top-level error boundary). | `461495f7` |
| AW | 30 | recorder-agent/availability | Adversary-on-`AV`: `AV`'s `read_timeout` only bounds the RESPONSE read. A large cache-file upload buffers the whole body and WRITES it first; if the controller/proxy accepts the connection then stops draining the body, the write blocks on backpressure forever and `read_timeout` never fires (no read in progress) — the exact upload-loop hang `AV` targeted, on the write half. Added an overall per-request `.timeout()` (env `RAKKR_CONTROLLER_REQUEST_TIMEOUT_MS`, generous 1h default so it never caps a legitimately-progressing multi-GB upload) bounding the whole exchange. The twin `AV` opened. Rust red→green (with a long read_timeout, only the overall deadline aborts a stalled exchange within the guard). | `6394b7ce` |
| AV | 29 | recorder-agent/availability | Fresh sweep: the controller HTTP client (`controller_http.rs`) built `reqwest::Client` with NO `.timeout`/`.connect_timeout`/`.read_timeout` (reqwest defaults to none) and no call site wrapped requests, so a black-hole controller/proxy (accepts the TCP connection, never responds) would hang the heartbeat / upload / job-poll loop FOREVER — the node looks alive but silently stops syncing until restart. Added `connect_timeout` + `read_timeout` (read_timeout bounds the wait between response bytes, so a legitimately-progressing large upload isn't aborted), env-tunable (`RAKKR_CONTROLLER_CONNECT_TIMEOUT_MS`/`_READ_TIMEOUT_MS`, defaults 10s/60s). Rust red→green (request to a never-responding listener errors within the read timeout instead of hanging). | `7bc22cd6` |
| AU | 29 | upload/disk-leak | Adversary-on-`AR`: `AR` stopped deleting the raw master on `deleteCacheAfterUpload` (fixing the data-loss), but the reconcile persists `cachePath: undefined`, and every reclaim gate (`recordingHasCachedFile` → retention candidate sizing, retention delete, explicit delete) keyed only off `cachePath` — so the preserved raw file became **unreclaimable forever** (a silent disk leak of the largest rendition, contradicting `AR`'s own "retention still reclaims raw" comment). The twin `AR` opened. Added `recordingHasReclaimableCache` (any rendition path + terminal status) and used it in the three reclaim gates + `recordingCacheFileSize`; `recordingHasCachedFile` stays `cachePath`-based for playback/download. Red→green (a raw-only uploaded recording is now sized as a retention candidate + deletable). | `0aab8cfc` |
| AS | 28 | recorder-agent/durability | Closes the confirmed `R28-STATE-ATOMIC`. `write_job_state_snapshot` + `save_manifest` used `fs::write` (truncate-then-write); a hard crash mid-write left a torn `state.json`, and on restart `read_job_state`'s decode error propagated → `reconcile_previous_recording_job_state` swallowed it and kept `recovery_pending=true`, so the daemon re-read and re-failed the same file every heartbeat forever, stranding the interrupted recording. Now both writers use a sibling temp file + atomic `rename`, and `read_job_state` treats an undecodable file as "no recoverable state" (log + `Ok(None)`) so a pre-existing torn file self-heals on the next write. Rust red→green (torn `state.json`). | `7f44833c` |
| AQ | 28 | upload-destination/store error-mapping | The `AP` class sweep found the upload-destination store had the same defect: `create` ran a bare insert and routed a `23505` on an operator-supplied duplicate `id` through the unconditional `failover()` → `503 database_unavailable` for a client conflict. Fixed identically to `AP` (mirror room/switcher): `UploadDestinationStoreError` + explicit-id pre-check (both backends) + `23505` discrimination → `409` in the route. Red→green. The store constraint→503 class is now swept (room, switcher, upload-destination correct; every other store confirmed clean). | `5d8a860e` |
| AN | 26 | schedule/roster RBAC | Adversary-on-`AK`: `AK`'s reorder made `reconcileScheduleRoster` run UNCONDITIONALLY before the schedule-existence-gated `update` (pre-`AK` the roster write was inside `if (reconciled)`). A schedule deleted concurrently (between `list()` and the `update`) then got fresh `source="calendar"` roster rows the delete cascade had already removed — orphaning `view`/`operate` grants that nothing heals (a retry skips the now-absent schedule). Now persist `roomId` first and only reconcile the roster when `update` confirms the schedule still exists; on a roster-reconcile failure, roll `roomId` back so a retry re-detects the move and re-runs both writes (keeps `AK`'s retry-heal, closes the orphan hole). Red→green (vanished-schedule + retry-heal). | `8e306100` |
| AK | 25 | schedule/roster RBAC | `reconcileNodeSchedules` (the `R2-RECONCILE-ATOMICITY` best-effort loop landed in the batch) persisted `schedule.roomId` **first**, then reconciled the calendar roster; if the roster reconcile threw (a store blip), the schedule was left committed to the NEW room with its roster stranded in the OLD one — and a retry PUT then **skipped** the schedule (`nextRoomId === schedule.roomId` already matched), so the stale roster never healed (assignees kept calendar `view`/`operate` on the wrong room). The fix's own comment claimed the retry was idempotent; it was not. Now reconciles the roster FIRST and persists `roomId` only after it succeeds (roomId is the commit point), so a retry re-detects the pending move and re-runs BOTH writes → an internally-consistent stale state that self-heals. Red→green (two-PUT harness). | `5afe1e7d` |
| D | 1 | recording/authz | Ad-hoc start skipped the per-room check when the selection resolved to no room (room-less node + unassigned channels), letting a roster-only operator capture unowned channels → require role/grant node authority (roster-blind) when `captureRoomId` is undefined, matching the room-less schedule rule | `87d370d7` |
| G3 | 2 | room/resilience | Postgres room delete mapped a genuine DB outage to `409 room_in_use` (the catch swallowed `DatabaseUnavailableError`) → rethrow it so the onError boundary returns 503 | `3b5e0eb5` |
| H | 2 | web/roster | Room roster (and schedule) subject picker fetched access-groups at the 50-row default (groups 51+ unreachable) and shared a bare `["access-groups"]` cache slot with the limit-200 management views (view-order-dependent list) → shared `subjectPicker*` helpers: limit 200 + params-suffixed keys | `3b5e0eb5` |
| R2-DEDUPE | 2 | scheduler/health | Fix `C`'s cross-room defer opened a fresh health event every retry tick for a *permanent* misconfiguration → dedupe against an already-open event (matches the watchdog active-event guard) | `3b5e0eb5` |
| X | 13 | node-secrets/atomicity | `PostgresNodeSshCredentialStore.persistActive` revoked the prior credential then inserted the new one in two separate statements with NO transaction, and the table had only a non-unique `node_id` index — so (a) two concurrent rotations could each insert an active row, leaving multiple un-revoked credentials for one node (violating "one active key per node"; a rotated-away key stays valid), and (b) a failed insert after the revoke left the node with ZERO active keys (SSH access lost). Added a partial unique index `node_ssh_credentials_active_node_idx WHERE revoked_at IS NULL` (migration `0041`) + wrapped revoke+insert in a transaction: atomic, and a racing second insert fails closed. Red→green via a DB-gated 16-way rotation-race test (`node:test-db`). | `f28744e0` |
| Z | 14 | agent-contract/DoS | `PUT /recordings/:id/cache-file` (`agent-routes.ts`) buffered the whole request body via `arrayBuffer()` with only an empty-body check — no size ceiling — so a compromised node credential could stream an unbounded body and exhaust controller memory (the sibling monitor-chunk route bounds its body; this one did not). Added a configurable ceiling (`RAKKR_RECORDING_CACHE_MAX_BYTES`, default 4 GiB): a Content-Length fast-reject plus a streaming bounded read (`agent-cache-upload-body.ts`) that aborts a chunked/lying-length body past the cap, both `413`. Red→green unit test on the bounded reader. | `8c516af1` |
| AA | 15 | agent-contract/DoS | The sibling of `Z`, surfaced by the adversary-on-`Z` pass (cf. `M`→`N`). `POST /nodes/:id/listen/chunk` (`agent-monitor-routes.ts`) also buffered the whole body via `arrayBuffer()` and only THEN checked size (its cap was post-hoc, not preventive) — a compromised node credential could stream an unbounded body and exhaust memory before the check. Now uses the same bounded streaming reader (512 KiB) so an over-cap body aborts early with `413`; the `<44`-byte lower bound still `400`s. Red→green + the size-boundary test the route previously lacked. | `6623ed36` |
| AB | 16 | settings/authz (IDOR) | Upload destinations are resource-scoped (`scopedUploadDestinations` filters them; the destination routes gate on the destination target), but the upload-policy create/update routes stored the body-supplied `destinationId` foreign key with NO existence or scope check — and recording-create scopes the policy itself, not the policy's destination. A caller could bind a policy to a scoped-out destination (routing recordings to a destination they were access-policy-denied) or to a non-existent one. Policy create/update now reject an unknown destination (`400`) or one the caller lacks resource-scope for (`403`), mirroring recording-create's policy-scope check (`hasResourceScope` + `uploadDestinationStore` wired into the policy routes). Same foreign-id-not-scope-checked class as the room findings. Red→green. | `b230cd9b` |
| AC | 17 | settings/authz (IDOR) | The last instance of the `AB` class, found by generalizing it into a sweep. Channel-map templates are resource-scoped (`scopedChannelMapTemplates`), and the three assignment/plan write routes (`PUT channel-map-assignments`, `PUT …/bulk`, `POST channel-map-assignment-plans`) scope-checked the assignment TARGET (`denyHiddenAssignmentTargets`) and the template's EXISTENCE — but never the template's scope. A caller could bind a scoped-out template to a target they own, applying channel mappings for a template they cannot see. Added a shared `denyHiddenTemplate` guard (mirrors `AB` + the target check) so all three routes reject a scoped-out template with `403`. Red→green. The recording/schedule foreign-id refs (node/profile/uploadPolicy) were re-confirmed scoped, and `retentionPolicy` is not resource-scoped so its reference needs no check — the foreign-id-scope class is now swept. | `51910972` |

### Low

| ID | Run | Area | Fix | Commit |
| --- | --- | --- | --- | --- |
| BF | 40 | observability/metrics | Test-coverage critic: `escapeLabel` (`metrics.ts`) neutralized `\\`, `"`, and `\n` in Prometheus label values but NOT `\r`. Node `alias`/`location.room` are operator-set and validated only as non-empty strings, so a raw carriage return can reach a label — and a bare `\r` emits a metric line that strict Prometheus/OpenMetrics parsers reject, breaking the **whole `/metrics` scrape** (denial-of-observability). Added the missing `\r`→`\\r` escape, matching the existing `\n` handling. Resolves the low-realism `R40-METRICS-CR-ESCAPE`. Red→green via a metrics test asserting no raw CR appears in the exposition for an alias containing one. | `19df7807` |
| BD | 38 | schedule/calendar correctness | Adversary-on-`BC` confirmed BC clean across all recurrence modes + case (b) + DST, but found its last residual: when a recurring series has NO persisted `nextRunAt` (a long `pause` exception can exhaust the ~10-year forward scan, leaving `nextRunAt=null` while `enabled` stays true), BC's `before.nextRunAt ?? undefined` parity anchor fell back to `undefined` → the every-N parity re-derived from wall-clock `now` — the exact phase-flip BC set out to kill — if an operator drags an occurrence in a calendar window past the pause. Fall back instead to the DRAGGED occurrence's own start (`body.data.occurrenceStartAt`), i.e. the phase the calendar rendered, so the recompute is `now`-independent; no-op for the populated case (`??` short-circuits). Closes the catalogued `R38-NULL-ANCHOR-PARITY-FLIP`. Reachability is exotic (>10yr scan-gap), hence Low. Red→green via a `mock.timers` route test with a null-`nextRunAt` bi-weekly series (pre-fix `2026-07-06`, post-fix `2026-07-13`). | `2aee46c0` |
| AP | 27 | switcher/store error-mapping | Fresh sweep: `PostgresSwitcherStore.create` ran a bare insert and routed ANY error — including a `23505` unique-violation on an operator-supplied duplicate `id` — through `failover()`, which unconditionally throws `DatabaseUnavailableError` → the route returned `503 database_unavailable` (misleading: a retry never succeeds) for what is a client conflict. The room store already handled this class (`R3-FK-RACE`); the switcher store was the outlier (imported neither `isPgErrorCode` nor a conflict pre-check). Added a `SwitcherStoreError` + an explicit-id conflict pre-check (both backends) + `23505` discrimination in the catch, mapped to `409` in the route (other store errors still rethrow to the `503` boundary). Red→green. | `743336c6` |
| AO | 26 | schedule/validation | Adversary-on-`AL`: routing `scheduleChannelSelectionFailure` through `effectiveCaptureInterfaceId` (`AL`) meant the process-wide `RAKKR_AGENT_CAPTURE_INTERFACE_ID` — which can name an interface absent on a given node — made validation hard-reject (`409 schedule_interface_not_found`) a schedule whose selection is valid on the node's real first interface, while `resolveScheduleRoom` silently accepted it against the node default (the two now CONTRADICTED). `effectiveCaptureInterfaceId` now honors the env id only when it exists on the node, else falls through to the first interface, so validation, room attribution, and the runtime capture target all agree. Also resolves the catalogued `R25-IFACE-ENV-ABSENT` attribution edge. Red→green (env naming an absent interface). | `1aea85eb` |
| AL | 25 | schedule/validation | `scheduleChannelSelectionFailure` (`schedule-route-helpers.ts`) validated the channel selection against `node.interfaces[0]`, while its sibling `resolveScheduleRoom` — and the recorder runtime — resolve the capture interface via `effectiveCaptureInterfaceId` (explicit id → `RAKKR_AGENT_CAPTURE_INTERFACE_ID` env → first). The `R2-IFACE-ENV` centralization (landed in the batch) fixed `resolveScheduleRoom` but left this sibling on `interfaces[0]`. When the controller pins a non-first capture interface via env, a schedule with no explicit interface was validated against the WRONG interface — falsely accepting a selection that fails at capture, or falsely rejecting one valid on the captured interface. Now uses `effectiveCaptureInterfaceId` so validation, room attribution, and runtime all agree. Red→green. | `8b165e5d` |
| W | 12 | settings/validation | The `recording-profiles` + `watchdog-policies` **create** schemas derived from the permissive base schema (`.partial().required({name})`), so `POST` accepted input the `PATCH` update schema rejects: a name past the `varchar(160)` column budget (→ 500/latch on insert, the Fix `S` class but operator-triggered), plus over-cap `bitrateKbps`/durations. Now both create schemas derive from the SAME bounded writable schema the update routes use — one source, no drift. (Recording-profile schemas extracted to a shared `recording-profile.ts` module to stay under the LOC guard.) | `6d0b3bcc` |
| Y | 13 | switcher/transport | The raw-TCP switcher line transport (`switchers/transport.ts`) accumulated a command's response in an unbounded buffer (`buffer += chunk`), and the control channel is unauthenticated telnet — so a compromised or spoofed device on the LAN could stream bytes continuously (each chunk resets the idle timer), growing memory without bound until the multi-second hard timeout, and the config-snapshot route would return the whole blob. Added a per-command response ceiling (default 1 MiB); an over-cap response fails fast with `switcher_response_too_large`. Red→green with a loopback flood server. | `f28744e0` |
| V | 12 | web/label-picker | The recordings + health pages fetched schedules with no `limit`, so the list route's server default (50) capped the schedule id→name label map AND the schedule filter dropdown — the 51st+ schedule's recordings/health events rendered with an unresolved label and could not be filtered by. The schedule twin of Fix `H` (access-group picker) and the node-picker `G76` fix → added `schedulePickerFilters()` (limit 200, mirrors `nodePickerFilters`), wired into both pages. | `6d0b3bcc` |
| E | 1 | node/validation | `assertAssignmentsBelongToNode` bounded the channel index by `channelCount`, not the actual channel set → a within-count phantom index was a silent no-op and a real channel whose index exceeds `channelCount` was wrongly rejected → validate against the interface's real channel index set | `87d370d7` |
| AG | 21 | schedule/robustness | `previewScheduleOccurrences` (`schedule-engine.ts`) lacked the `Number.isNaN(Date.parse(...))` guard its sibling `windowScheduleOccurrences` has, so a malformed `nextRunAt` flowed into `new Date(NaN).toISOString()` → `RangeError: Invalid time value`, crashing the preview and its switcher/calendar callers. Added the matching guard to break gracefully. Low (needs a corrupted `nextRunAt`), but a real crash + an inconsistency with the codebase's own defensive posture. Red→green. | `f634c043` |
| R2-INVAL | 2 | web/staleness | Channel-room reassignment (Fix `B` reconciles schedules + rosters server-side) did not invalidate the room-roster / schedule-calendar / schedules / room-overview client caches → stale views until refetch → added the four `invalidateQueries` calls | `3b5e0eb5` |
| L | 3 | schedule/roster | `move-occurrence` created a one-off clone but never `reconcileScheduleRoster`(clone), so the moved occurrence's room calendar roster was not populated (unlike every other schedule create) — a consistency/robustness gap (access held via the same-room original until it is removed) → threaded `reconcileScheduleRoster` into the calendar routes + reconcile the clone | `4146211c` |

### Also fixed (gate, not a product finding)

| Item | Run | Fix | Commit |
| --- | --- | --- | --- |
| LOC budget | 1 | Fix A pushed `schedule-routes.ts` past the 1000-LOC guard (base was exactly 999) → extracted the audit-failure recorders to `schedule-route-audit.ts` | `87d370d7` |
| Q (docs) | 6 | `docs/how-to/manage-rooms.md` claimed room delete always cascade-cleans every reference; corrected to note a schedule reference blocks the delete with 409 (the Fix G1 behavior) | `d1fc5c98` |
| LOC budget | 4 | Fix M pushed `index.ts` past 1000 → extracted the per-room meter/monitor access helpers to `meter-room-access.ts` | `9e865b06` |
| LOC budget | 12 | Fix W's writable schema pushed shared `index.ts` past 1000 → extracted the recording-profile schemas to `recording-profile.ts` (mirrors `watchdog-policy.ts`); the create-cap test went to its own `settings-create-cap-routes.test.ts` to keep `settings-routes.test.ts` under budget | `6d0b3bcc` |
| Flaky format-check | 25 | The `R2-PRECEDENCE` test (`auth-access-routes.test.ts`) wrote its roster grant + room to the default `data/room-roster.json`/`rooms.json` instead of a temp dir (unlike the sibling room tests). Because `mise run check` runs `node:test` and `node:format-check` in **parallel** (`depends`), the unformatted leaked `room-roster.json` intermittently failed the format-check gate (and left an untracked artifact). Isolated `RAKKR_ROOM_STORE_PATH` + `RAKKR_ROOM_ROSTER_STORE_PATH` to the test's temp dir. | `4cd25154` |
| LOC-split refactor | 25 | User-requested proactive pass: split **every production-source + Rust file >900 LOC** into cohesive modules (18 files → new sibling modules; pure refactors, unchanged public exports, verified by tsc / API+web tests / cargo test+clippy+rustfmt / all baseline verifiers). New baseline-verifier + baseline-doc entries added for the moved recorder-agent test/code snippets. | `210df0de`, `821a38c7`, `3e5e8e40`, `bf0d513a` |

### Catalogued backlog — cleared (post-convergence batch)

After the run loop converged, the entire catalogued/coverage backlog below was
worked through and landed, each with its own red→green proof and gates. The four
real deferred bugs (`R20-*`, `R2-RECONCILE-ATOMICITY`, `R3-FK-RACE`) plus the
security/resilience trio (`R8-DBLATCH`, `R18-LOGIN-TIMING`, `R14-HEALTH-FATAL`,
`R19-STALE-FALLBACK-PERMS`) were fixed; the coverage gaps got their locking tests.
The atomicity/race items were made deterministically testable with DB-gated
high-concurrency tests (registered in `run-db-integration-tests.mjs`).

| ID | Sev | What landed | Commit |
| --- | --- | --- | --- |
| R20-UPLOAD-QUEUE-RMW | High | `succeed`/`fail`/`retry` wrapped in a tx with `SELECT … FOR UPDATE` (tx threaded through `findItem`/`writeItem`); DB-gated 16-item concurrent retry+succeed test | `5de60b12` |
| R20-SETTINGS-RMW | Low | `update()` serialized via `pg_advisory_xact_lock(hashtext('controller-settings'))` (singleton may not exist for `FOR UPDATE`); DB-gated 20-round concurrent-field test | `430f282f` |
| R2-RECONCILE-ATOMICITY | Low | per-schedule try/catch in `reconcileNodeSchedules` (log + continue), `reconcileFailedScheduleIds` added to the audit | `a06a5102` |
| R3-FK-RACE | Low | `PostgresRoomStore.delete` detects SQLSTATE 23503 → `RoomStoreError room_in_use` (409, not a false 503). Also made `isPgErrorCode` walk the drizzle `.cause` chain; DB-gated FK-race test | `a0bed820` |
| R8-DBLATCH | Med | `markDatabaseUnavailable` re-throws on a constraint error (SQLSTATE class 22/23) instead of latching to the memory store; new `isPgConstraintError` + red→green classifier test | `c29deb9f` |
| R18-LOGIN-TIMING | Med | `login()` always runs one scrypt (a cached decoy hash when no account/hash applies) so an unknown email is not faster than a real failed login; timing red→green test | `cf71b170` |
| R14-HEALTH-FATAL | Med | heartbeat-loop health updates made best-effort via `apply_tick_health_updates` (log + count, never `?`-propagate); Rust test drives an unwritable health sink and asserts the tick survives | `cc42d507` |
| R19-STALE-FALLBACK-PERMS | Med | bounded fallback grace (`RAKKR_AUTH_FALLBACK_GRACE_MS`, default 15m): after a configured DB is unavailable past the grace, `authenticate()` probes for recovery and otherwise denies stale cached sessions; pure `dbOutageGraceExceeded` unit test + behavioral test. (LOC: extracted `auth-persistence-helpers.ts`) | `eb781eb3` |
| R2-IFACE-ENV | Low | single `effectiveCaptureInterfaceId` helper (explicit → `RAKKR_AGENT_CAPTURE_INTERFACE_ID` → first interface) shared by `resolveScheduleRoom` + the 3 runtime capture paths; test asserts the env-selected interface's room | `5236bbf1` |
| R1-SSE | Low | `/meter-events` routes each emit through `resolveVisibleMeterFrame` (node-scope → resolve node → strict per-channel filter, else emit nothing); tested via the extracted helper (SSE loop not drivable in-harness) | `bff2e3ff` |
| R3-GROUP-ROSTER-COV | Med | end-to-end test wiring the real roster store into the group-delete route (group grant cascade-cleaned, user rows survive) | `527a5c90` |
| R5-OVERVIEW-COV | Med | shared-node two-room `/overview` isolation test (only the room's own recordings + occurrences) | `b2353880` |
| R3-ROSTER-PUT-COV | Low | roster PUT room-scope (404 unknown room) + round-trip + fail-inert bogus subject | `b9296cb6` |
| R1-SHRINK | Low | reconcile shrink+regrow test: vanished channel's room dropped, re-grown unassigned, survivor kept | `e5382f66` |
| R1-PG | — | DB-gated `PostgresNodeStore.assignChannelRooms` round-trip (assign, re-read via a fresh store, clear) | `a15f946d` |
| R2-PRECEDENCE | High | exported `permissionDecision`; test locks that a user- and group-scoped access-policy DENY beats a room-roster grant (`access_policy_denied`, roster fallback skipped) | `8dfd3e53` |

---

## Open work

### Catalogued / suspected

| ID | Sev | Kind | Locus | Note |
| --- | --- | --- | --- | --- |
| R3-OIDC-GROUP | ✅ resolved-on-main | by-design | `oidc-sync.ts` (`oidcGroupsFromClaims`) | **RESOLVED by PR #25** (`923397f0`, another session): OIDC group claims now slug onto the SAME id space as operator-created groups by design (`accessGroupSlug`), formalizing the IdP-group-sync trust model this item flagged, with a fake-IdP test harness, groups-overage handling, and lenient claim parsing. Audited on rebase — sound. |
| R25-SSE-GATE | Low | ✅ resolved | `node-routes.ts` / `meter-room-access.ts` | **RESOLVED by `BL`** (`77c38f48`): the `/meter-events` gate now uses the roster-inclusive `resolveScopedNode` (as `/meters` does), so a roster-only room operator gets their own channels' stream instead of an empty one. |
| R25-IFACE-ENV-ABSENT | ✅ resolved | fixed | `room-resolution.ts` (`effectiveCaptureInterfaceId`) | **RESOLVED by `AO` (Run 26):** `effectiveCaptureInterfaceId` now honors the env capture-interface id only when it exists on the node, else falls through to the first interface — closing both this attribution edge and the `AL` validation twin. |
| R26-CHUNK-RENDITION | Low | ✅ resolved | `agent-cache-uploads.ts` (`handleChunkUpload`) | **RESOLVED by `BH`** (`d2e4c740`): the chunk completion block is now gated on `!supplementary` (mirroring the whole-recording path), so a `rendition=raw` supplementary chunk carrying a `chunkTotal` can't complete the job or flip the recording to cached. Red→green. |
| R26-ACCESS-TX | Low | ✅ resolved | `auth-service.ts` (`persistLocalUserAccess`) | **RESOLVED by `BG`** (`c6a145b8`): the DELETE-then-INSERT of roles/grants/groups is now wrapped in `db.transaction`, so a failed INSERT rolls the DELETEs back and the user keeps their prior access. DB-gated red→green. |
| R27-RECONCILE-DOUBLE-FAIL | Low | ✅ resolved | `channel-room-routes.ts` (`reconcileNodeSchedules`) | **RESOLVED by `BJ`** (`ee4120dd`): the roomId rollback is now wrapped in its own try/catch; on a double failure (reconcile threw AND rollback threw) it logs at error + flags the schedule in `inconsistent[]`, and the route records a distinct `nodes.channel-rooms.reconcile.inconsistent` failed audit event — the stranded state is now observable/recoverable instead of silent. Red→green. |
| R27-UI-BARE-CANMANAGE | Low | ✅ resolved | `node-inventory-dialogs.tsx` (`NodeConfigureDialog`) | **RESOLVED by `BK`** (`fb47ed22`): `NodeConfigureDialog` now takes a real `canManage` prop threaded from `nodes.tsx`'s `node:manage`-derived permission, instead of hardcoding `true` for its child editors. |
| R28-CALENDAR-PARITY | ✅ resolved | fixed | `schedule-engine.ts` (`windowScheduleOccurrences`) | **RESOLVED by `AT` (Run 28):** an optional `parityAnchor` decouples the every-N parity anchor from the scan cursor, so the calendar window measures parity from the schedule's true `nextRunAt` phase (default behavior for all other callers unchanged). |
| R28-STATE-ATOMIC | ✅ resolved | fixed | recorder-agent `state.rs`, `recorder_cache_retention.rs` | **RESOLVED by `AS` (Run 28):** both writers now use a sibling temp file + atomic `rename`, and `read_job_state` treats an undecodable file as "no recoverable state" so a pre-existing torn file self-heals. |
| R28-CREATE-UPSERT | Low | ✅ resolved | `upload-policies.ts` (`writePolicy`), `settings-store.ts` (`writeChannelMapTemplate`) | **RESOLVED by `BL`** (`77c38f48`): upload-policy + channel-map-template CREATE now pre-check the id and throw a typed store error → `409` (mirroring `AP`/`AQ`) instead of `onConflictDoUpdate` overwriting an existing row; update keeps its upsert. Red→green. |
| R28-DST-END | Low | ✅ resolved | `schedule-engine.ts` (`scheduleRecordingDurationSeconds`) | **RESOLVED by `BJ`** (`ee4120dd`): capture length is a date/tz-independent schedule constant threaded widely, so the DST-correct rewrite would need a broad signature refactor. Resolved as documented intentional behavior — a design comment (fixed-length capture; ±1h on the ~2 DST-transition days is accepted as predictable) + a test pinning the fixed 3h duration for a 01:00–04:00 spring-forward window. Closes the undocumented/untested concern. |
| R28-CAL-CHIP-TZ | Low | ✅ resolved | web `calendar-grid.ts`, `schedule-occurrence-routes.ts`, shared | **RESOLVED by `BM`** (`62074a8a`): added `timezone` to `ScheduleCalendarOccurrence` (populated from `schedule.timezone`); `groupByLocalDay` now buckets chips by the schedule-local day (Intl zoned-day key, mirroring `occurrenceLocalDateIso`) when present, browser-local fallback otherwise. Red→green. |
| R28-DEDUP-FAILED | Low | ✅ resolved | `upload-queue.ts` (`reusableUploadQueueItem`) | **RESOLVED by `BH`** (`d2e4c740`): dropped terminal `failed` from `activeStatuses`, so a re-enqueue after a terminal failure creates a fresh queue item instead of returning the stale failed one and skipping the upload. Red→green. |
| R29-CHUNK-FINAL | Low | ✅ resolved | `upload-runner.ts` (`chunkedRecordingFinalization`) | **RESOLVED by `BH`** (`d2e4c740`): finalization now treats `captureDone && total === undefined` (completeness unprovable — the total-bearing final chunk never attached) as `partial` rather than overstating `uploaded`. Red→green. |
| R25-UPLOAD-RESURRECT | Low | ✅ resolved | `upload-queue.ts` (`updateItem`) | **RESOLVED by `BH`** (`d2e4c740`): the runner's succeed/fail/retry writes now go through a new UPDATE-only `updateItem` (WHERE by id) instead of `INSERT … ON CONFLICT DO UPDATE`, so a status write racing a `deleteForRecording` can no longer re-INSERT (resurrect) a just-deleted queue row — it updates zero rows and no-ops. Red→green. |
| R25-S3-NOVERIFY | Low | ✅ resolved | `upload-runner.ts` (`uploadWasVerified`), `upload-executor.ts` | **RESOLVED by `BH`** (`d2e4c740`): cache deletion is now gated on a per-item verification map — `uploadWasVerified(status)` is true only for `matched`/`provider_validated` (genuine read-back), not `provider_declared`. So an unverified custom-S3 upload still completes/uploads but its controller cache is **retained** (never deleted on an unproven upload), threaded through `resolveCacheDeletion` + `deleteChunkCacheIfPolicyAllows`. Red→green. |
| R33-API-READINESS | Medium | ✅ resolved | `status-routes.ts` (`/readyz`), `index.ts`, `api-deployment.yaml` | **RESOLVED by `BN`** (`a11db001`): added an unauthenticated `GET /readyz` that returns `503`/`200` from `checkDatabaseReady()` — a dedicated readiness DB client running `select 1` raced against a 3s timeout (always-true in memory-store/test mode). The API `readinessProbe` now points at `/readyz` (liveness stays on `/healthz`, correct — a DB outage shouldn't restart the pod). Red→green (`status-readyz.test.ts` + `deploy-api-health-probe.test.ts`). |
| R33-MIGRATE-JOB | Medium | ✅ resolved | `migration-job.yaml`, `api-deployment.yaml`, `_helpers.tpl` | **RESOLVED by `BN`** (`a11db001`): all three coupled defects fixed together — (1) the API init-migrate container is now gated on `and .Values.api.migrateOnStartup (not .Values.migrations.job.enabled)`, so enabling the Job makes the init-migrate exclusive; (2) the Job gained the same `wait-for-database` initContainer as the deployment; (3) the Job carries `helm.sh/hook: pre-install,pre-upgrade` so it sequences migration-before-serving. A `_helpers` `validateMigrationJob` guard requires `postgres.enabled=false` (the pre-install hook can't run against bundled Postgres), forcing the Job path to external DB only. The default init-container path is unchanged. Render-verified via `helm:check`. |
| R33-HELM-RENDER-GATE | Low | ✅ resolved | `.mise.toml`, `scripts/verify-helm-render.mjs` | **RESOLVED by `BN`** (`a11db001`): pinned `helm@3.16.3` in `.mise.toml` and added a `helm:check` task (`scripts/verify-helm-render.mjs`) that actually renders the chart across `secrets.backend` values and asserts the render-level invariants (the migration-Job hook/exclusivity/wait-for-db + readiness-probe wiring above), wired into `mise run check`. Cross-object render bugs are now caught by CI instead of needing a hunter to hand-install helm. |
| R38-OIDC-EMAIL-LINK | High | ✅ resolved | `oidc-user-sync.ts`, `oidc-sync.ts`, `schema.ts` | **RESOLVED by `BE`** (operator-directed): OIDC email-based linking let a token whose email matched the local-admin row merge onto it and inherit owner. Now linked on the verified IdP subject (`users.external_id`, migration `0044`), with an email-collision refusal and `email_verified:false` rejection — the local-admin takeover is closed. See the `BE` fix row. |
| R40-ENHANCE-TAIL | Low | **CONFIRMED** (needs rig/audio verification) | recorder-agent `enhance.rs` (`dfn_process`/`rnnoise_process`) | The **enhanced** rendition clips ~20 ms of tail audio and leads the raw master by ~20 ms: DeepFilterNet3's `DfTract::process` is a streaming model with a `~2-frame` (hop 480 = 10 ms) algorithmic lookahead; `dfn_process` reads `enhanced` immediately per hop and then `out.truncate(samples.len())`, so (a) the output is time-shifted ahead of the input and (b) the final ~20 ms of real denoised speech stays trapped in the model's rolling buffers and is discarded by the truncate — the last words of a recording's enhanced copy can be cut. RNNoise has an analogous ~10 ms head fade-in artifact. **The RAW master (source of truth) is always preserved intact**, so no recording is lost — only the denoised convenience rendition has boundary artifacts, hence Low. The engine tests only assert `len` + RMS drop, so alignment/tail are unchecked. Fix (needs Linux + tract engine + a real-audio red→green — the audio quality change deserves rig validation per the project's "meters reflect real life" ethos, so NOT landed on this Windows env): after the input loop, feed `lookahead` zero hops to flush the delayed audio, then drop the LEADING `lookahead*hop` output samples instead of truncating the trailing ones (align output to input); for RNNoise, discard the first synthesized frame per the crate docs. |
| R39-OIDC-RACE-502 | Low | ✅ resolved | `oidc-user-sync.ts` (`linkOidcUser`/`writeOidcRow`), `auth-service.ts` | **RESOLVED by `BG`** (`c6a145b8`): `linkOidcUser` now catches the subject `23505` and retries `findUserByExternalId` once (the concurrent-first-login loser now attaches to the winner's row instead of 502ing); `writeOidcRow` maps the email-unique `23505` to an `OidcSyncError("oidc_email_conflict")` (clean status, not opaque 502); and `memoryRecordByEmail` now defensively `.toLowerCase()`es its lookup. DB-gated race red→green (`oidc-race-conflict.test.ts`). |
| R37-CONCURRENT-STATE | Medium | suspected | recorder-agent `state.rs`, `recording_job_recovery.rs`, `recorder_cache_retention.rs` (`save_manifest`) | Carries forward prior-audit **G61** (`docs/internal/audits/2026-07-02-gap-hunt.md`, catalogued Low/config-gated) + **G51b-race1** (manifest race), with a Run-37 reachability re-assessment. `spawn_recording_job_workers` (`main.rs:472/646`) spawns `max_concurrent_recordings` concurrent capture workers, but every state writer (`write_job_state`, `write_recoverable_job_state`, `write_upload_checkpoint_state`, chunked) targets the SINGLE `config.agent_state_file`, and each atomic write fully overwrites it. With `maxConcurrentRecordings ≥ 2` (a first-class controller node-capacity setting, `z.number().int().positive().max(128)`, demo=8) two recordings run at once; a crash/restart then recovers only the LAST writer's job — the other in-flight recording(s) are orphaned on disk (untracked by retention) until the controller's lease-expiry fails them. The shared cache-manifest has the same lost-update shape (leaks an untracked cache file). **Default is safe** (`RAKKR_MAX_CONCURRENT_RECORDINGS` `default_value_t = 1` → single worker), so it's config-gated as G61 stated — but reachable via a documented capacity setting, hence Medium not Low. Deferred (not a new finding): the correct fix is a substantial concurrency refactor — per-job state files (`state-<jobId>.json`) + recovery iterating all non-terminal files + a manifest lock/merge — the slice G61's title anticipates. |
| R36-ADMIN-DENY-COUNT | Low | ✅ resolved | `apps/api/src/health-routes.ts` (`visibleHealthEvents` path) | **RESOLVED by `BL`** (`77c38f48`): collapsed the unrestricted branch onto the scoped `visibleHealthEvents` path (as the restricted branch already did), so an owner/admin who is the SUBJECT of an access-policy DENY now sees `meta.total` and the tile `summary` scoped to the events they can actually see — header, tiles, and table rows all agree. Chose strict agreement (count reflects what they can SEE) over raw existence. Red→green. |
| R35-ERROR-BOUNDARY | Low | ✅ resolved | `apps/web/src/main.tsx` (`RouteErrorBoundary`, `createRouter`) | **RESOLVED by `BK`** (`fb47ed22`): added a `RouteErrorBoundary` wired as the router's `defaultErrorComponent`, so a render throw in one route is contained to a "section failed — retry" card instead of unmounting the whole console mid-monitoring. |
| R34-ORPHAN-POSTGRES | Low | ✅ resolved | `_helpers.tpl` (`validateDatabase`), `docs/operations/deployment.md` | **RESOLVED by `BI`** (`b9495096`): added a `validateDatabase` `fail` guard that errors when `postgres.enabled && (database.externalUrl || database.existingSecret.name)` — so configuring an external DB while leaving bundled Postgres on now fails the render loudly instead of silently orphaning a StatefulSet + PVC. Render-tested (`deploy-orphan-postgres.test.ts`); the deployment-doc WARNING was widened to name the orphan outcome. |
| R33-WEB-CACHE | Low | ✅ resolved | `deploy/nginx/default.conf.template` (`location /`) | **RESOLVED by `BI`** (`b9495096`): `location = /index.html` now sends `Cache-Control: no-cache` (so a controller-image bump can't strand a stale SPA shell), hashed `location /assets/` is marked `immutable`, and `gzip on` + `gzip_types` compresses JSON/asset responses. Test `deploy-web-cache.test.ts`. |
| R32-PROXY-BUFFERING | Low | ✅ resolved | `deploy/nginx/default.conf.template` (`location /api/`), Helm `ingress.annotations` | **RESOLVED by `BI`** (`b9495096`): set `proxy_request_buffering off` on `/api/` (so nginx forwards streaming instead of spooling the whole body — the API's Content-Length over-cap fast-path is no longer defeated at the proxy) and, as the paired slice required, bumped `proxy_read_timeout`/`proxy_send_timeout` to `3600s` for the now-concurrent upstream write; the same `nginx.ingress.kubernetes.io/proxy-request-buffering: "off"` + timeout annotations were added to the Helm ingress. Test `deploy-proxy-streaming.test.ts`. |

_All other catalogued/coverage items were cleared in the post-convergence batch
above (see "Catalogued backlog — cleared"). As of the operator-directed
catalogued-backlog sweep (`BG`–`BN`, below), every catalogued/suspected/coverage
entry in this table is ✅ resolved except the two Rust items (`R40-ENHANCE-TAIL`,
`R37-CONCURRENT-STATE`), which are deferred pending Linux + audio-rig validation
(the agent's real capture/enhance path does not build or run on this Windows
env)._

---

## Verified clean (checked, no bug)

Confirmed correct by direct source review and adversarial verification during Run 1:
the per-channel room-resolution helpers `channelRoomId` / `nodeRoomIds` /
`interfaceRoomIds` / `resolveSelectionRoom` (fallback to node default, cross-room
`ok:false`, empty-selection handling); the per-level scope-target expansion in
`scope-targets.ts` / `resource-scope-targets.ts` (a channel resolves to its own room,
an interface/node to the union, and single-room resources use `addNodeResourceTargets`
so a node grant authorizes without widening the room scope — no `"room"` branch means a
bare room target does not over-expand); the room-capability roster mapping
(`roomCapabilityAuthorizes` only grants when the target resolves to a rostered room);
the meter per-channel filter `filterMeterFrameForUser` (drops other rooms' channel
levels); the ad-hoc recording **create** path (already re-authorizes against the
resolved capture room and rejects cross-room selections); the schedule **create** path
(gates `requirePermission` on the channel-resolved room, rejects cross-room); and DB
migration `0040` / `schema.ts` for the room columns (no schema↔migration drift found).

---

## Run log

Following [`docs/contributing/audit-workflow.md`](../../contributing/audit-workflow.md).
Target: **5 consecutive clean runs.** A run is *dirty* if it changes any file, a gate
fails, a new confirmed finding surfaces, or `main` advanced with un-audited commits →
the streak resets to 0. Runs are strictly sequential; parallel read-only hunters run
within a run.

| Run | Date | Focus | New confirmed | Fixed | Clean? | Streak |
| --- | ---- | ----- | ------------- | ----- | ------ | ------ |
| 1 | 2026-07-03 | per-channel room ownership (PR #23): RBAC/scope, recording + schedule attribution, node persistence, regression blast-radius | A, B, C, D, E | 5 | no | 0 |
| 2 | 2026-07-03 | adversary-on-Run-1 fixes + room roster/access-policy + room lifecycle/cascade + web room boundaries | F, G1, G2, G3, H, R2-DEDUPE (+ R2-INVAL) | 7 | no | 0 |
| 3 | 2026-07-03 | adversary-on-Run-2 fixes + roster PUT/access-groups/OIDC + occurrence/recording-room lifecycle + web picker residuals | J, K, L | 3 | no | 0 |
| 4 | 2026-07-03 | lean hunt-only sweep: recording consumption (read/download/delete) + health/metrics + deep RBAC re-sweep + agent routes + completeness critic | M | 1 | no | 0 |
| 5 | 2026-07-03 | adversary-on-M + retention/upload/switcher per-room + room-overview + broad convergence sweep | N | 1 | no | 0 |
| 6 | 2026-07-03 | adversary-on-N node-scope-authority CLASS sweep + broad convergence + PR #24 docs-vs-behavior (`main` advanced to `bed15ae4`) | O, P, Q | 3 | no | 0 |
| 7 | 2026-07-03 | exhaustive class final + broad convergence + fresh-outsider (`main` advanced to `923397f0` via PR #25 OIDC) | R | 1 | no | 0 |
| 8 | 2026-07-03 | PR #25 OIDC surface audit + broad convergence (`main` advanced to `ccffb002` via PR #26 docs before the run; stable during it) | S | 1 | no | 0 |
| 9 | 2026-07-03 | convergence: adversary-on-recent + fresh full-surface sweep | — (R1-SSE re-surfaced, catalogued) | — | **yes** | 1 |
| 10 | 2026-07-03 | systematic RBAC route×permission×scope×audit enumeration + IDOR/cross-resource-linkage sweep | — | — | **yes** | 2 |
| 11 | 2026-07-03 | recording/chunked lifecycle + data-loss/upload/retention + schedule concurrency/TOCTOU | U | 1 | no | 0 |
| 12 | 2026-07-03 | agent-facing + web console + settings/switcher/watchdog input-validation dimension | V, W | 2 | no | 0 |
| 13 | 2026-07-03 | upload runner/secrets + node onboarding/SSH credentials + switcher transport + time/scheduling math | X, Y | 2 | no | 0 |
| 14 | 2026-07-03 | Rust recorder-agent (cache/jobs, sync/health, capture/enhance) + controller agent-contract | Z (+ R14-HEALTH-FATAL catalogued) | 1 | no | 0 |
| 15 | 2026-07-03 | convergence: adversary-on-recent-fixes + web-console depth + fresh-outsider + untouched modules | AA | 1 | no | 0 |
| 16 | 2026-07-03 | convergence: store parity + DB/schema drift + Rust-agent remainder + cross-resource IDOR + fresh-outsider | AB | 1 | no | 0 |
| 17 | 2026-07-03 | foreign-id existence+scope-check CLASS sweep (generalizing AB) + adversary-on-AB + fresh | AC | 1 | no | 0 |
| 18 | 2026-07-03 | fresh angles: multi-write atomicity + auth/token/session + deep web console + arithmetic/parsing | AD (+ R18-LOGIN-TIMING catalogued) | 1 | no | 0 |
| 19 | 2026-07-03 | convergence: adversary-on-AD + remaining atomicity + session lifecycle + controller runners + fresh | AE (+ R19-STALE-FALLBACK-PERMS catalogued) | 1 | no | 0 |
| 20 | 2026-07-03 | read-modify-write race CLASS sweep (generalizing AE) + adversary-on-AE + fresh | — (R20-UPLOAD-QUEUE-RMW, R20-SETTINGS-RMW catalogued) | 0 | no | 0 |
| 21 | 2026-07-03 | clean-attempt: fresh-outsider over controller / shared+DB / web+runners, excluding all swept+catalogued classes | AF, AG | 2 | no | 0 |
| 22 | 2026-07-03 | class sweeps generalizing AF (deserializer field-drop) + AG (unguarded date-parse) + broad fresh | — | 0 | **yes** | 1 |
| 23 | 2026-07-03 | convergence: broad fresh-outsider over controller / shared+DB+agent / web+runners | AH | 1 | no | 0 |
| 24 | 2026-07-03 | DB schema-width CLASS sweep (generalizing AH) + broad fresh (controller / agent / web) | — | 0 | **yes** | 1 |
| 25 | 2026-07-04 | resumed on new worktree off merged `main` (`25873566`); audit the 16-item backlog batch **as landed code** (auth-resilience trio, RMW/tx fixes, capture-iface centralization, R14 health) + broad fresh, 4 parallel hunters | AI, AJ, AK, AL (+ flaky-gate fix; `R25-*` catalogued) | 4 | no | 0 |
| 26 | 2026-07-04 | adversary-on-Run-25-fixes (AI/AJ/AK/AL) + split-refactor behavior-safety + broad fresh, 3 parallel hunters | AM, AN, AO (`R26-*` catalogued) | 3 | no | 0 |
| 27 | 2026-07-04 | adversary-on-Run-26-fixes (AM/AN/AO) + fresh least-swept surface (OIDC/crypto/bootstrap/ansible/DB/shared/web) + web permission boundaries, 3 hunters | AP (`R27-*` catalogued) | 1 | no | 0 |
| 28 | 2026-07-04 | AP-class sweep (store constraint→503) + adversary-on-AP + broad convergence fresh-outsider, 3 hunters | AQ, AR, AS, AT fixed (all confirmed findings closed); R28-CREATE-UPSERT + 3 suspected catalogued | 4 | no | 0 |
| 29 | 2026-07-04 | clean-attempt: adversary-on-Run-28-fixes (AQ–AT) + fresh least-covered ground (Rust audio pipeline, agent-contract edges), 2 hunters | AU (AR-twin disk leak), AV (controller HTTP no-timeout hang); R29-CHUNK-FINAL suspected | 2 | no | 0 |
| 30 | 2026-07-04 | clean-attempt: adversary-on-Run-29-fixes (AU/AV) + fresh convergence, 2 hunters | AW (AV-twin: upload write-stall unbounded); fresh pass EMPTY (surface quiet); AU clean | 1 | no | 0 |
| 31 | 2026-07-04 | clean-attempt: adversary-on-Run-30-fix (AW) + fresh convergence over least-covered ground (DB schema/migration drift, shared↔DB width parity, deploy config) + full route permission-gate completeness sweep, 3 hunters | AX (deploy 1 MB body cap 413s every cache-file upload); adversary-on-AW CLEAN; route-gate sweep EMPTY (95+ mutations / 39 families, 0 ungated/unaudited) | 1 | no | 0 |
| 32 | 2026-07-04 | deploy/ops dimension (opened by AX): adversary-on-AX + fresh sweep of the rest of the deploy surface (ansible runner/role, Dockerfiles, compose, Helm templates/probes, release-artifact contract), 2 hunters | AY (web liveness/readiness probe on API-proxied /healthz crash-loops the web tier); adversary-on-AX found the `R32-PROXY-BUFFERING` twin (catalogued, suspected — fix is a slice) | 1 | no | 0 |
| 33 | 2026-07-04 | thorough deploy/ops CONVERGENCE (flush the vein AX/AY opened): k8s pod-lifecycle + AY-adversary, nginx request-path, Helm chart render (helm 3.16.3 run live), 3 hunters | AZ (RollingUpdate over RWO PVC → upgrade deadlock), BA (documented externalSecrets recipe boots broken: bundled-PG empty password ≠ app DATABASE_URL); nginx path CLEAN (confirmed); catalogued R33-API-READINESS, R33-MIGRATE-JOB, R33-HELM-RENDER-GATE, R33-WEB-CACHE | 2 | no | 0 |
| 34 | 2026-07-04 | clean-attempt: adversary-on-Run-33-fixes (AZ/BA) + cross-surface completeness critic + agent↔controller JSON-contract sweep, 3 hunters (interrupted by a usage limit mid-run, resumed post-reset) | **none confirmed** — AZ CLEAN + BA recipes correct (helm-render-verified); completeness critic CLEAN (13 modalities); JSON contract CLEAN (8 payloads end-to-end); catalogued R34-ORPHAN-POSTGRES | **yes** | **1** |
| 35 | 2026-07-04 | fresh convergence over two less-audited angles: web-console runtime correctness + controller resource-lifecycle, 2 hunters | BB (health summary tiles undercount — page-local not global); resource-lifecycle CLEAN (20 memoized pools <100, 6 guarded runners, handles close); catalogued R35-ERROR-BOUNDARY | no | 0 |
| 36 | 2026-07-04 | generalize BB: "page-local aggregate that should be global" CLASS sweep across all pages/endpoints + adversary-on-BB, 2 hunters | **none confirmed** — class fully swept (jobs G74 + health BB were the only 2 instances; every other page server-computes/full-queries/honest-labels its aggregates), adversary confirms BB CLEAN; catalogued R36-ADMIN-DENY-COUNT (pre-existing) | **yes** | **1** |
| 37 | 2026-07-04 | fresh convergence over two less-recently-swept angles: Rust agent capture/job/cache state machine + scheduler recurrence/timezone math, 2 hunters | BC (move-occurrence re-anchors every-N parity to now — silent wrong-time shift); Rust pass re-surfaced prior-audit G61 (concurrent-recording shared state file) → catalogued R37-CONCURRENT-STATE (config-gated, deferred refactor); otherwise both surfaces sound | no | 0 |
| 38 | 2026-07-04 | adversary-on-BC (all recurrence modes + case b + DST) + fresh auth/OIDC/crypto security pass, 2 hunters | BC verified CLEAN, its last residual closed by BD (null-anchor parity flip); auth pass surfaced CONFIRMED security `R38-OIDC-EMAIL-LINK` (email-based OIDC linking → local-admin owner takeover, catalogued, needs product call) — crypto/token/session/OIDC-flow otherwise hardened | no | 0 |
| — | 2026-07-04 | operator-directed fix of R38-OIDC-EMAIL-LINK | `BE` — OIDC linked on the verified IdP subject (`users.external_id`, migration 0044), email-collision refused, email_verified:false rejected; DB-gated red→green | (fix) | 0 |
| 39 | 2026-07-04 | adversary-on-BE (OIDC subject-linking security) + fresh convergence over least-swept surfaces (switcher subsystem, upload executor, metrics), 2 hunters | **none confirmed** — BE verified SOUND (no takeover/bypass/regression); switcher+upload+metrics EMPTY (all traced clean); catalogued R39-OIDC-RACE-502 (low coverage) | **yes** | **1** |
| 40 | 2026-07-04 | fresh: test-coverage completeness critic + recorder-agent remainder (enhance/bootstrap/ALSA), 2 hunters | BF (metrics escapeLabel missed \r → scrape-break) fixed; R40-ENHANCE-TAIL (enhanced rendition ~20ms tail-clip, confirmed, catalogued — safe fix needs rig/audio verification); bootstrap/device-selection/enhance-buffer-sizing verified sound | no | 0 |

### Notes on the runs

- **Run 1** targeted PR #23's fresh surface with six read-only hunters (room RBAC/scope,
  recording attribution, schedule attribution, node channel-room persistence,
  shared-contracts/DB, and regression blast-radius) plus an adversarial verify pass on
  every lead. 13 leads → 11 survived verification → deduped to 5 distinct confirmed bugs
  (the schedule-PATCH escalation `A` was independently found by three hunters). All 5
  fixed with red→green proof. New **SUSPECTED/coverage** leads surfaced and logged, not
  fixed: `R1-SSE` (meter-events SSE filter — cosmetic, not a reachable leak), `R1-SHRINK`
  (reconcile-shrink room-assignment drop — untested intentional behavior), and `R1-PG`
  (Postgres channel-room round-trip coverage). Dirty (landed 5 fixes) → streak 0.
- **Run 2** turned the adversary on Run 1's own fixes plus roster/access-policy, room
  lifecycle/cascade, and web room boundaries (four hunters + verify). 12 leads → 11
  survived → deduped to 6 confirmed fixes. The headline is `F`: Run 1's Fix `A` left a
  room-less hole (it only re-checked when the new room was *set*), so the adversary pass
  paid off directly. Room-delete parity (`G1`/`G2`/`G3`) and the web roster picker (`H`)
  were fresh. Also hardened my own Run-1 additions (`R2-DEDUPE` health-event flood,
  `R2-INVAL` client invalidation). New items logged, not fixed: `R2-PRECEDENCE` (High
  coverage — roster→permission deny-precedence is correct but untested; heavy harness
  deferred with a recipe), `R2-IFACE-ENV` and `R2-RECONCILE-ATOMICITY` (Low, catalogued).
  Dirty (landed 6+ fixes) → streak 0.
- **Run 3** turned the adversary on Run 2's fixes plus roster PUT / access-groups / OIDC,
  the occurrence + recording-room lifecycle, and residual web pickers (four hunters).
  The hunt found 8 leads, but a **session limit killed 6 verify agents mid-run**; the 2
  fully-verified survivors were `J` (room-detail hooks crash — HIGH) and `K` (the G3
  FK-race residual — Low). The 6 dropped leads were then verified **directly against
  source** (main-loop): `L` (move-occurrence roster reconcile — Low, fixed) held up; the
  group-delete→roster cascade was found already wired + called (`auth-group-routes.ts:273`),
  so it plus the OIDC-group-collision and roster-PUT leads are catalogued
  (`R3-*`, coverage/suspected), not bugs. `J` also motivated enabling
  `react-hooks/rules-of-hooks` repo-wide (a permanent guard for the whole class).
  Dirty (landed J, K, L) → streak 0.
- **Run 4** was a lean **hunt-only** sweep (5 hunters, no verify fan-out — the main loop
  verified, after Run 3's verify phase hit a session limit) over the **consumption** side
  (recording read/download/delete/export/metadata, health, metrics), a systematic RBAC
  route re-sweep, agent-facing routes, and a completeness critic. All five hunters
  converged on a SINGLE confirmed bug — `M`, the listen-monitor whole-node audio leak —
  and found nothing else across the consumption/RBAC/agent/completeness surface (a strong
  hardening signal). `M` had been flagged by an earlier audit but never landed; this run
  closed it (per-room monitor gate + `meter-room-access.ts` extraction). Dirty (landed M)
  → streak 0. Convergence trend in confirmed fixes/run: 5 → 7 → 3 → 1.
- **Run 5** turned the adversary back on `M` and swept retention/upload/switcher per-room,
  the room overview, and a broad convergence pass. It found `N` — the same listen/meters
  cross-room leak, reopened through a DIFFERENT auth path (`meterRoomAccess` treated any
  node-scope, including a room-derived one, as whole-node authority). This predated `M`
  and also affected the meters filter, so it was worth a distinct CRITICAL fix. Retention,
  upload, and switcher were confirmed to have NO room coupling (no findings); the room
  overview was confirmed correctly scoped (logged as `R5-OVERVIEW-COV`, a coverage gap,
  not a bug). Dirty (landed N) → streak 0. The lesson: two adversary passes on the same
  fix (`M` then `N`) were needed because the leak was reachable via two auth paths.
- **Run 6** generalized `N` into a CLASS sweep: any code treating node-level resource
  scope (`hasResourceScope({node})`, which expands to the room union) as full-node
  authority. It found two more instances — `O` (`scopedRooms` room-list metadata) and `P`
  (`/metrics` per-channel meter + listen telemetry, HIGH) — both fixed by reusing
  `hasFullNodeAuthority`. `main` advanced mid-run (PR #24, docs-only how-to guides);
  rebased onto `bed15ae4`, which surfaced `Q` (a `manage-rooms.md` claim contradicting the
  Fix G1 409 behavior). Retention/upload/switcher and the room overview were re-confirmed
  sound. Dirty (landed O, P, Q) → streak 0. This class (`N`/`O`/`P`) is now swept across
  all known consumers; a `resource-scope-targets`-level fix (never expanding a node target
  to its room union for authority checks) would be the deeper structural hardening, but the
  per-consumer `hasFullNodeAuthority` gate closes every reachable path.
- **Run 7** was the convergence attempt: an exhaustive final class sweep, a broad
  re-read, and a from-scratch fresh-outsider isolation pass. The broad and fresh-outsider
  passes found NOTHING (the surface is converged). The class sweep found one last instance
  — `R`, the schedule-scoped health-event room-union expansion in `resource-scope-targets.ts`
  (the structural, scope-target-level variant of the class). Fixed with red→green. But
  `main` advanced DURING the run (PR #25, OIDC group-collision fix + fake-IdP harness), so
  per the clean-run rule Run 7 is NOT clean regardless of findings. Rebased onto `923397f0`
  and audited the OIDC surface on rebase (see `R3-OIDC-GROUP` — resolved by PR #25, sound);
  the full suite (now +PR #25 OIDC tests) stays green. Dirty (landed R; main advanced) →
  streak 0. `main` has now advanced in two consecutive runs — if that pace continues,
  convergence needs the merge queue to quiesce (the methodology's coordinate/freeze case).
- **Run 8** rebased onto PR #26 (docs, auto-merged with Fix `Q`) then adversarially
  audited PR #25's NEW OIDC code plus a convergence pass. The convergence pass found
  nothing (surface still converged); the OIDC audit found `S` — an uncapped group-claim
  display name vs the `varchar(160)` column that could latch the auth service into memory
  fallback (login DoS). Fixed with red→green. `main` did NOT advance during Run 8, so this
  run is dirty only from landing `S` — the merge queue has (for now) quiesced. With the
  OIDC surface now audited and the room-ownership surface converged, the next run is the
  real convergence attempt. Dirty (landed S) → streak 0.
- **Run 9 — FIRST CLEAN RUN (streak 1).** Adversary-on-recent-fixes + a fresh full-surface
  pass. The fresh full-surface pass found NOTHING. The adversary pass re-surfaced the
  `/meter-events` SSE meter filter gap — but that is `R1-SSE`, a LOW, `RAKKR_DEMO_METERS`-
  gated coverage item with NO production leak (production streams nothing); it is catalogued,
  not fixed (a listed coverage gap does not break cleanliness, and the SSE-stream+demo
  integration test is impractical in-harness). No CONFIRMED bug and no actionable finding
  required a change; the run made ZERO code/test changes, gates stayed green, and `main`
  stayed current at `ccffb002`. Clean → streak 1. (Its only new SUSPECTED/coverage lead —
  `R1-SSE` — is explicitly listed above, per the clean-run rule.)
- **Run 10 — CLEAN (streak 2).** A systematic RBAC enumeration (every route's
  permission gate / scope target / denial audit / scoped visibility, weighted to non-room
  surfaces: settings, uploads, retention, watchdog, node-lifecycle, ssh-credential,
  bootstrap, switcher, audit, agent) plus an IDOR / cross-resource-linkage sweep. BOTH
  hunters returned ZERO findings. No changes, gates green, `main` current at `ccffb002`.
  Clean → streak 2.
- **Run 11 — DIRTY, streak reset to 0.** A core-correctness sweep (recording/chunked
  lifecycle + data-loss/upload/retention, and schedule concurrency/TOCTOU) — a dimension
  outside the room-ownership focus of Runs 1–10. Schedule concurrency found nothing; the
  recording-lifecycle hunter found `U` — a genuinely NEW HIGH (retry orphans upload-queue
  items → retention pinned + spurious cache_path_missing events), not a room-isolation
  issue and not in the G-series backlog. Fixed with red→green. This is the diverse-iteration
  value: a fresh dimension surfaced a real bug the 10 prior runs never probed. The streak
  resets to 0 (a dirty run audited a different tree), and the 5-clean count restarts.
- **Run 12 — DIRTY, streak stays 0.** Continuing the diverse-dimension rotation onto the
  agent-facing + web-console + settings/switcher/watchdog **input-validation** surface
  (untouched by Runs 1–11). Two LOW confirmed bugs, both an asymmetry-with-the-update-path
  class: `W` — the settings recording-profile/watchdog **create** schemas were built off the
  permissive base schema, so `POST` accepted an over-`varchar(160)` name (a 500/latch on
  insert, the operator-triggered cousin of Fix `S`) and over-cap numerics the `PATCH` schema
  rejects; fixed by deriving both create schemas from the same bounded writable schema update
  uses. `V` — the recordings/health pages fetched schedules with no `limit`, so schedule
  id→name labels + the schedule filter dropdown silently dropped everything past the default
  50-row page (the schedule twin of Fix `H` and the `G76` node-picker fix); fixed with a
  `schedulePickerFilters()` helper. Both red→green. Landing `W`'s single-source writable
  schema pushed shared `index.ts` over the LOC guard → extracted `recording-profile.ts`
  (mirroring `watchdog-policy.ts`). Switcher config was swept and found sound (no input-cap
  or per-room gaps). Dirty (landed V, W) → streak 0. The pattern from Runs 11–12 holds: once
  the room-ownership surface converged, rotating to genuinely new dimensions keeps surfacing
  real (if lower-severity) bugs — convergence to 5 clean requires each remaining dimension to
  come back empty, which the input-validation dimension now has after this fix.
- **Run 13 — DIRTY, streak stays 0.** Four read-only hunters over untouched dimensions:
  the controller-only upload runner + destination secrets, node onboarding / SSH credentials /
  bootstrap tokens, switcher driver/transport, and time/scheduling occurrence math. The
  time-scheduling hunter returned NOTHING (that dimension is converged). Three raw leads; on
  main-loop verification: (1) **rejected** the upload-runner "stub-only recording never
  finalizes to uploaded" lead — it is by-design and the *safe* behavior: the stub filter is
  intentional, the default retention is `action:"keep"`, and promoting a no-real-destination
  recording to `uploaded` would make never-uploaded data eligible for deletion (a data-loss
  bug); staying `cached` is correct fail-safe. (2) **confirmed `X`** — the SSH-credential
  rotation atomicity gap (non-transactional revoke+insert, no partial unique index → multiple
  or zero active credentials under concurrency/failure); fixed with migration `0041` + a
  transaction, red→green via a DB-gated concurrency test (the first audit fix to add DB-backed
  coverage, run under `node:test-db`). (3) **confirmed `Y`** — the unbounded switcher-transport
  response buffer over an unauthenticated telnet channel; fixed with a response ceiling. Dirty
  (landed X, Y) → streak 0. Secrets/switcher/transport is now swept; time/scheduling came back
  empty. The remaining un-swept dimension of note is the Rust recorder-agent internals.
- **Run 14 — DIRTY, streak stays 0.** Four hunters over the last big un-swept dimension: the
  Rust recorder-agent (cache/jobs, sync/health, capture/enhance) and the CONTROLLER side of the
  agent contract. The capture/enhance hunter returned NOTHING (converged). Five raw leads; on
  main-loop verification: **rejected** two — the crash-recovery chunk-total `uploaded + pending`
  addition (`recording_job_recovery_chunk_total.rs`) is not reachably triggerable (chunk counts
  never approach `u32::MAX`), and the startup inventory-reconcile "fail-open" is by-design/safe
  (the hunter itself noted it). **Confirmed + fixed `Z`** — the controller `cache-file` upload
  buffered an unbounded body (memory-exhaustion DoS from a compromised node credential), fixed
  with a Content-Length fast-reject + a streaming bounded read, red→green unit test. **Confirmed
  + catalogued `R14-HEALTH-FATAL`** — the heartbeat loop exits the daemon on a LOCAL health-log
  write failure (`?` on `update_meter_health`/`update_system_health`); narrower than first flagged
  (controller-sync failures are already handled gracefully, so controller downtime does not kill
  agents), and the resilient fix is a cross-cutting agent-loop change not cleanly red→green in
  the harness, so it is catalogued with a recipe. Dirty (landed Z) → streak 0. The Rust agent's
  capture/enhance path and the time/scheduling math are now the two dimensions that have come
  back empty; the health-loop resilience and the cache-jobs/sync paths still hold a catalogued
  item apiece.
- **Run 15 — DIRTY, streak stays 0 (but the surface is clearly near-converged).** A convergence
  run: adversary-on-recent-fixes, a from-scratch web-console deep read, a fresh-outsider
  full-surface pass, and the not-yet-deeply-audited controller modules (recording-cache path
  construction, listen-session/audit/health stores, secret-box crypto). THREE of the four hunters
  found NOTHING — the web console, the fresh-outsider sweep, and the untouched-modules pass all
  came back empty (no path-traversal in cache resolution, no XSS/secret-leak, no missing gate/audit,
  no crypto misuse). The adversary-on-`Z` pass found `AA`: the monitor-chunk upload was the exact
  sibling of `Z` (buffer-then-check), the same "fix one instance, an adversary pass finds the
  twin via a different route" pattern as `M`→`N`. Fixed by reusing the `Z` bounded reader; the
  DoS class (unbounded agent upload body) is now swept across both agent upload routes. Dirty
  (landed AA) → streak 0. With `AA` closing the last known instance of that class and 3/4 hunters
  empty, the next run is a genuine attempt at the first clean run of a new streak.
- **Run 16 — DIRTY, streak stays 0 (convergence continues; 3/4 hunters empty again).** Targeted the
  least-trodden remaining surface: Postgres-vs-memory store parity + shared-schema/DB-column drift,
  the Rust-agent remainder (bootstrap/config/command-templates/worker lifecycle), cross-resource
  IDOR, and a final fresh-outsider pass. THREE hunters found NOTHING (store parity, Rust remainder,
  fresh-outsider — no backend divergence, no schema drift, no key-wipe/injection/panic, no missing
  gate). The IDOR pass found `AB`: the upload-policy `destinationId` foreign key was stored without
  an existence or resource-scope check, while destinations ARE scoped and recording-create only
  scopes the policy (not its destination) — the same foreign-id-not-scope-checked class as the room
  findings, on a fresh surface. Fixed symmetrically (reject unknown `400` / out-of-scope `403`),
  red→green. Dirty (landed AB) → streak 0. Two consecutive convergence runs (15, 16) each surfaced
  exactly one real finding on a genuinely different surface — the tail is thinning but not yet dry.
- **Run 17 — DIRTY, streak stays 0.** Rather than a broad convergence pass, this run GENERALIZED
  `AB` into an exhaustive class sweep (the `N`→Run-6 move): find EVERY body/path-supplied foreign-id
  reference bound without an existence+scope check where the referenced type is itself resource-scoped.
  The recording/schedule foreign-id hunter came back EMPTY (node/profile/uploadPolicy already scoped;
  `retentionPolicy` is not resource-scoped, so no check needed) and the fresh-outsider pass was empty.
  The settings/switcher hunter and the adversary-on-`AB` hunter INDEPENDENTLY converged on the SAME
  last instance — `AC`: the channel-map template assignment/plan routes scope-checked the target and
  the template's existence but not the template's scope. Fixed with a shared `denyHiddenTemplate`
  guard across all three write routes; red→green. Dirty (landed AC) → streak 0. The foreign-id-scope
  class (rooms → `AB` destinations → `AC` templates) is now swept across every scoped-type reference.
  Two independent hunters landing on one finding, plus two empty hunters, is a strong convergence
  signal — the next run is again a genuine attempt at the first clean run.
- **Run 18 — DIRTY, streak stays 0.** With the known classes swept, this run opened FRESH angles:
  multi-write atomicity, auth/token/session lifecycle, a second deep web-console pass, and
  arithmetic/parsing edges. The web-console and arithmetic/parsing hunters found NOTHING (converged).
  The atomicity sweep found `AD` — the twin of `X` on the node API bearer credentials: the exact
  same non-transactional revoke-then-insert with no partial unique index (an insert failure locks
  the node out; a race leaves a stale valid token). Fixed identically to `X` (transaction + partial
  unique index, migration `0042`, DB-gated race test). The auth pass found `R18-LOGIN-TIMING` — a
  login timing side-channel (disabled/no-local-password users skip scrypt) that enables user
  enumeration; real but info-disclosure-only, and its constant-time fix is not cleanly red→green
  in-harness, so catalogued with a recipe. Dirty (landed AD) → streak 0. The credential-rotation
  atomicity class (`X` SSH + `AD` API bearer) is now swept across both credential stores. The
  audit has now closed six bug classes to completion; remaining open items are all catalogued
  (mostly not-harness-testable resilience/timing).
- **Run 19 — DIRTY, streak stays 0.** Convergence pass: adversary-on-`AD` + remaining atomicity,
  auth session lifecycle, controller runners, and a final broad fresh read. The controller-runners
  and broad-fresh hunters found NOTHING (converged). The adversary/atomicity hunter first confirmed
  `AD` is safe (the new `node_credentials` partial unique index does not break `seed()`/`enroll()` —
  seed skips existing nodes and never inserts credentials; enroll is fresh-node-only), then found
  `AE`: `heartbeat()` and `update()` both read-modify-write the node metadata JSONB without a row
  lock, so a frequent agent heartbeat clobbers a concurrent operator edit (last-writer-wins data
  loss). Fixed with `SELECT … FOR UPDATE` transactions serializing the RMW per node; red→green via a
  DB-gated 16-heartbeat-vs-update race test. The session-lifecycle hunter found
  `R19-STALE-FALLBACK-PERMS` — the auth memory-fallback serves login-time permissions during a DB
  outage (a facet of the catalogued `R8-DBLATCH` resilience design), catalogued not fixed (product
  tradeoff, not cleanly testable). Dirty (landed AE) → streak 0. Run 19's findings were again in the
  concurrency/resilience space; the remaining open items are all catalogued.
- **Run 20 — DIRTY (no code landed), streak stays 0.** Generalized `AE` into a read-modify-write
  class sweep across all Postgres stores, plus an adversary-on-`AE` (deadlock/perf check) and a
  broad fresh pass. The adversary-on-`AE` and broad-fresh hunters found NOTHING — importantly, the
  adversary confirmed `AE`'s new `SELECT … FOR UPDATE` locks are single-row and deadlock-free, and
  that `assignChannelRooms`/`reconcileInterfaces` do not share the clobber. The RMW hunters found
  two more instances of the class — `R20-UPLOAD-QUEUE-RMW` (upload-queue succeed/fail/retry; runner
  vs operator, High) and `R20-SETTINGS-RMW` (controller-settings singleton, Low). A correct
  `SELECT … FOR UPDATE` fix was drafted for the upload-queue path but REVERTED and catalogued
  instead: unlike `AE`'s cleanly-observable field revert, these are probabilistic lost-operation
  races whose deterministic red→green needs multi-item high-concurrency setup the module-singleton
  store makes unreliable — so per the session's discipline (land a fix only with a real red→green;
  otherwise catalogue with the proven recipe) both were catalogued, with `AE` as the reference
  implementation for a focused atomicity-hardening slice. Dirty (2 new confirmed findings) → streak 0.
  The RMW-race class is now enumerated (`AE` fixed; upload-queue + controller-settings catalogued).
- **Run 21 — DIRTY, streak stays 0.** A deliberate CLEAN attempt: three fresh-outsider hunters over
  the controller API, the shared+DB layer, and the web+runners, with EVERY swept and catalogued
  class (incl. the entire RMW-race class, timing, and resilience-fallback) explicitly excluded — so
  a finding could only be a genuinely NEW class. It still found three, in three fresh areas: `AF`
  (recorder-cache retention thresholds `maxAgeDays`/`maxBytes`/`minFreeDiskPercent` silently dropped
  on job deserialization → agent never runs deferred cache cleanup → unbounded recorder cache) and
  `AG` (a malformed `nextRunAt` crashes `previewScheduleOccurrences` with a RangeError, unlike its
  guarded sibling) were fixed red→green; the third — an OIDC discovery error-message "leak" — was
  REJECTED as a false positive (the route is `auth:manage`-only, and surfacing the discovery-fetch
  error to the admin configuring OIDC is intended, not a leak to an untrusted party). Dirty (landed
  AF, AG) → streak 0. Notably these were NEW classes (deserialization data-loss; unguarded date
  parse), showing the tail still holds distinct, non-concurrency defects — the audit remains
  productive at run 21.
- **Run 22 — CLEAN, streak 1 (first clean run of the Run 12–21 series).** Generalized the two Run 21
  veins into class sweeps: `deserializer-field-drop` (generalizing `AF` — every hand-rolled
  `*FromValue`/`*FromRow`/`*FromMetadata` mapper vs its schema) and `unguarded-date-parse`
  (generalizing `AG` — every `new Date(...).toISOString()` / `Date.parse` feeding arithmetic), plus
  a broad fresh pass. The date-parse and broad-fresh hunters found NOTHING. The deserializer hunter's
  sole "finding" was a RE-REPORT of already-fixed `AF` (same `recorderCacheRetentionFromValue`,
  despite the exclusion) — verified the fix is in place, so it is a hunter false-positive, not a new
  finding. No confirmed NEW bug, no change made, gates green, `main` current at `ccffb002`. Clean →
  streak 1. Both new classes (`AF` deserializer-field-drop, `AG` date-parse) came back empty on the
  generalized sweep, so they are swept, not just single-instance.
- **Run 23 — DIRTY, streak reset to 0.** Broad fresh-outsider convergence sweep (controller /
  shared+DB+agent / web+runners). The controller sweep was empty. Two findings: `AH` — a genuinely
  new-class defect (`recording_chunks.size_bytes` 32-bit `integer` overflow that fails a
  multichannel/uncompressed X32 WAV chunk with "value out of range") — fixed red→green (widened to
  `bigint`, migration `0043`); and a switcher-runner "inverted NaN-end semantics" lead, REJECTED as
  a false positive (corruption-only trigger, no crash, and defaulting a live route to "keep active"
  on a bad end time is a defensible fail-safe, not a bug). Dirty (landed AH) → streak 0. That the
  audit STILL found a real, distinct High (a DB column-width bug) at run 23 — one clean run short of
  building a streak — is the headline: the codebase has a long, diverse tail, so 5-consecutive-clean
  is not reachable within the MAX_RUNS=25 safety cap. See the convergence assessment below.
- **Run 24 — CLEAN, streak 1.** Generalized `AH` into a DB schema-width class sweep (every column vs
  the values its writer/schema can produce: other 2^31-overflowable `integer`s, over-`varchar`
  writers, enum mismatches) plus broad fresh passes over the controller, the recorder-agent, and the
  web console. The schema-width sweep found NOTHING (AH was the only instance — class swept), and the
  agent/web passes were empty. The controller pass's sole lead — "missing per-schedule audit event on
  the group-delete cascade" — was REJECTED: the privileged action (group delete) IS `auth:manage`-
  gated and audited (`auth.groups.delete.succeeded`), and that audit event records the cascade scope
  via `details.schedulesUpdated`; the per-schedule updates are deterministic side-effects (the roster
  cascade is silent too), so no unaudited privileged action exists. No confirmed bug, no change, gates
  green, `main` current. Clean → streak 1.
- **Run 25 — DIRTY, streak reset to 0 (audit resumed; operator re-issued the 5-clean mandate).** The
  Run-1–24 branch merged to `main` via PR #29, bringing BOTH the run-loop fixes AND the 16-item
  catalogued-backlog batch into `main` as landed code. Resumed on a fresh worktree off `main`
  (`25873566`) with the mandate renewed (so the run counter continues past the old `MAX_RUNS=25` and the
  cap resets for the resumed effort). The key realization: **the 16-item backlog batch was catalogued
  during the loop then implemented AFTER Run 24 — it had never been audited AS LANDED CODE.** So Run 25
  aimed four parallel read-only hunters at it: (1) the auth-resilience trio (R8-DBLATCH, R18-LOGIN-TIMING,
  R19-STALE-FALLBACK-PERMS), (2) the RMW/tx fixes (R20 upload-queue + settings, R3-FK-RACE,
  R2-RECONCILE-ATOMICITY), (3) the capture-interface centralization (R2-IFACE-ENV) + R1-SSE, and (4) the
  Rust R14-HEALTH-FATAL + a broad fresh-outsider sweep. It found FOUR real bugs, all in the freshly-landed
  batch: `AI` (High — R8's re-throw over-reached ~10 fire-and-forget/read callers → a valid login with a
  long X-Forwarded-For 401s), `AJ` (High — R14 only covered 2 of ~9 tick-loop health-append sites → daemon
  still crashes on a local append failure), `AK` (Medium — R2-RECONCILE's write order strands a roster and
  the retry can't heal it), and `AL` (Low — R2-IFACE-ENV missed the sibling validator). Plus a flaky-gate
  fix (the R2-PRECEDENCE test leaked an unformatted artifact into a directory a parallel format-check
  scans). R18/R19/R20/R3-FK-RACE and the rest were verified sound. Four `R25-*` suspected/by-design leads
  catalogued, no change. **Lesson: catalogued-then-batch-implemented fixes are un-audited surface — landing
  a fix from a recipe is not the same as auditing the landed code; three of the four Run-25 bugs are holes
  IN backlog fixes.** Separately (user-requested, not an audit finding), a LOC-split pass split every
  production-source + Rust file >900 LOC into cohesive modules (pure refactors, fully verified). Dirty
  (landed AI–AL + gate fix + the split refactor) → streak 0.
- **Run 26 — DIRTY, streak stays 0.** Adversary-on-Run-25-fixes (3 parallel hunters: adversary on
  AI/AJ/AK/AL, split-refactor behavior-safety, broad fresh). The **split-refactor safety** hunter
  verified all 18 extractions are behavior-preserving (api.ts key-set 117=117, Rust re-export paths +
  route precedence intact) — the LOC-split introduced NO drift. The **broad fresh** hunter found no live
  bug (upload/retention/switcher runners, control loop, watchdog, audit store, SMB/S3, recording-delete
  all sound) — one Low `R26-CHUNK-RENDITION` catalogued. The **adversary** hunter is the story: it found
  THREE real issues, two of them holes the Run-25 fixes THEMSELVES introduced — exactly the M→N pattern.
  `AM` (High): AJ made the health append best-effort but the same tick's idle recorder-cache sweep still
  `?`-propagated its manifest I/O failure → daemon crash (AJ's missed twin). `AN` (Medium): AK's reorder
  ran the roster reconcile unconditionally before the existence check, orphaning roster grants when a
  schedule is deleted mid-reconcile (a hole AK introduced). `AO` (Low-Med): AL's use of the env capture
  interface 409-rejected valid schedules on nodes lacking that interface and contradicted
  `resolveScheduleRoom` (a hole AL introduced; the fix also resolves catalogued `R25-IFACE-ENV-ABSENT`).
  All three red→green. AI's re-throw scoping was confirmed sound; one pre-existing SUSPECTED twin
  (`persistLocalUserAccess` non-transactional DELETE+INSERT) catalogued as `R26-ACCESS-TX`. Dirty (landed
  AM, AN, AO) → streak 0. **Lesson reinforced: adversary-on-own-fixes is the highest-yield pass — landing
  a fix routinely opens or leaves a twin; two of three Run-26 bugs were self-inflicted by Run 25.**
- **Run 27 — DIRTY, streak stays 0.** Adversary-on-Run-26-fixes (AM/AN/AO) + a fresh sweep of the
  least-audited surface (OIDC flow, secret-box/SSH crypto, Rust bootstrap, ansible runner + agent.sh,
  bootstrap/credential stores, DB migration drift, shared-vs-column bounds) + a web-console
  permission-boundary pass. The adversary pass found the Run-26 fixes had CONVERGED: `AM` and `AO` are
  complete twin-closures (every `?` in the heartbeat tick enumerated and classified non-fatal; all 5
  `effectiveCaptureInterfaceId` call sites consistent; env is controller-side only), and `AI` re-verified
  sound (an improvement). `AN` left one genuine but self-acknowledged narrow window (`R27-RECONCILE-DOUBLE-FAIL`:
  roster-throw AND rollback-throw → the AK bug in a double-store-failure window, bounded by the absence of
  a cross-store transaction) — catalogued, not fixed. The fresh sweep found the surface sound EXCEPT one
  Low outlier — `AP`: the switcher store mapped a duplicate-id `23505` to a 503 instead of 409 (the
  `R3-FK-RACE` class the audit already closed for rooms). Fixed red→green. The web boundary pass found no
  live bug (one non-exploitable defense-in-depth note, `R27-UI-BARE-CANMANAGE`). Dirty (landed AP) →
  streak 0. The Run-25/26 fix-chain has converged (AM/AO/AI clean); the remaining finding was an
  independent pre-existing outlier, not another self-inflicted twin.
- **Run 28 — DIRTY, streak stays 0.** Three hunters: an `AP`-class sweep (store constraint→503), an
  adversary-on-`AP`, and a broad convergence fresh-outsider. The `AP`-class sweep swept the class clean
  bar one un-fixed instance — `AQ` (upload-destination duplicate-id → 503), fixed like `AP` — plus an
  adjacent silent-upsert variant catalogued (`R28-CREATE-UPSERT`); adversary-on-`AP` found no hole. **The
  broad convergence pass is the headline: it found THREE genuinely new confirmed bugs the 27 prior runs
  missed** — `AR` (High, DATA-LOSS: `deleteCacheAfterUpload` destroyed the never-uploaded raw master),
  fixed red→green; `R28-CALENDAR-PARITY` (High: the calendar mis-anchors `interval>1` recurrence parity to
  the window edge, rendering the wrong days for every bi-weekly/every-N schedule); and `R28-STATE-ATOMIC`
  (Med: non-atomic `fs::write` of agent state strands an interrupted recording after a hard crash). Plus 3
  suspected (DST end-duration, calendar-chip TZ, upload dedup includes `failed`). All four confirmed bugs
  (AQ, AR, AS, AT) were fixed red→green in-run. Dirty (landed AQ, AR, AS, AT) → streak 0.
  **Assessment: the codebase has a genuinely long, diverse tail** — even at run 28 a fresh from-scratch
  pass surfaced two new High-severity user-visible/data-loss defects in never-audited corners (calendar
  recurrence math; enhanced-recording cache retention). All four confirmed findings were then landed
  red→green in-run: `AR` (data-loss), `AS` (`R28-STATE-ATOMIC`, atomic state write + torn-file recovery),
  and `AT` (`R28-CALENDAR-PARITY`, the parity-anchor decoupling — done cleanly via an optional param that
  leaves every existing caller byte-identical). So Run 28 closed ALL of its confirmed findings; only
  lower-confidence suspected items (`R28-CREATE-UPSERT` + 3) remain catalogued. 5-consecutive-clean is not
  near; the next run resumes the clean-attempt loop (adversary-on-Run-28-fixes + fresh), and convergence
  requires several successive fresh passes to come back empty.
- **Run 29 — DIRTY, streak stays 0.** A deliberate clean-attempt: adversary-on-Run-28-fixes (AQ–AT) + a
  fresh pass over the least-covered ground (Rust audio pipeline internals, controller↔agent contract
  edges). The adversary confirmed AQ/AS/AT are clean but found `AU` — the twin `AR` opened: `AR` correctly
  stopped deleting the raw master, but left the recording in a `cachePath: undefined` state that every
  reclaim gate filters out, so the preserved raw leaked on disk forever (fixed by a reclaim-aware
  predicate). The fresh pass found `AV` — the controller HTTP client had NO request timeout, so a
  black-hole controller hangs the agent's sync loop forever (fixed with connect/read timeouts). Both
  Medium, both red→green. One Low suspected catalogued (`R29-CHUNK-FINAL`). Dirty (landed AU, AV) →
  streak 0. Five consecutive clean-attempt runs (25–29) have each surfaced ≥1 confirmed bug — the tail is
  real and the fix-chain still occasionally opens a twin (AR→AU), so convergence to 5-clean is a
  multi-run/multi-session effort; every remaining confirmed finding has, however, been fixed in-run, so
  the tree carries no known open confirmed bug entering Run 30.
- **Run 30 — DIRTY, streak stays 0 (but the fresh dimension came back EMPTY).** Adversary-on-Run-29-fixes
  (AU/AV) + a fresh convergence pass. **The fresh convergence hunter found NOTHING** — it swept the
  least-machine-checked routes (listen/meters isolation, room-capability RBAC core, settings-scope
  symmetry, audit CSV), cross-resource consistency, web privileged controls/secret round-trips, and
  external/agent input parsing (command-template, filename sanitization, cache path containment, SMB
  traversal, monitor/node-config token routes, switcher driver parsing) and confirmed the surface is
  genuinely converged (two non-reachable observations, correctly not reported). The ONLY finding was
  again a self-inflicted twin: the adversary confirmed AU is clean but found `AW` — `AV`'s `read_timeout`
  bounds only the response read, leaving a large upload's request-body WRITE stall unbounded; fixed with
  an overall request timeout. Dirty (landed AW) → streak 0. **The signal is that the fix-chain, not the
  fresh surface, is now the only source of findings: Runs 29–30 each surfaced exactly one bug, and both
  were twins the immediately-preceding fix opened (AR→AU, AV→AW). Once the timeout fix stops opening
  twins (AW adds no new external behavior beyond a deadline), the next fresh+adversary pass has a real
  chance of being the first clean run.**
- **Run 31 — DIRTY, streak stays 0 (but two of three hunters came back clean/empty and the code
  surface is now genuinely converged).** Three hunters: adversary-on-Run-30's-fix (AW) + a fresh
  convergence pass over the least-covered ground (DB schema-vs-migration drift, shared-vs-DB width
  parity, deploy/Helm/nginx config) + a full route permission-gate completeness sweep. **AW verified
  CLEAN** — the overall request timeout is a generous, env-tunable, whole-exchange deadline that
  severs no legitimate long request or long-poll (every controller call is a bounded request/response;
  the job-poll is a client-side sleep loop, not a server-held long-poll), so the adversary fix-chain
  that ran AR→AU→AV→AW has finally stopped opening twins. The **route-gate sweep came back EMPTY**
  (95+ mutations across 39 route families, zero ungated, zero unaudited — corroborating the fresh
  pass's own full route-table completeness check). The single finding was `AX`, from the fresh pass —
  the **first deployment-topology/proxy-config bug of the audit**: the web tier's nginx and the Helm
  ingress both fell back to the 1 MB default body cap, so a recorder node pointed at the production
  ingress host got `413` on essentially every ≤4 GiB cache-file upload and the whole upload pipeline
  was broken in a standard Helm/reverse-proxied deploy — invisible to the Compose smokes, which bypass
  nginx and hit the API on `:8787` directly. Fixed by deferring both proxies to the controller's
  authoritative bounded cap (red→green via a config-invariant test). Dirty (landed AX) → streak 0.
  **Signal: the fix-chain closed (AW clean) and two independent completeness sweeps (the fresh
  route-table pass + the dedicated route-gate hunter) found zero code findings — the only remaining
  bug was in the deployment/proxy layer, the last un-audited surface. With AX closing it and no code
  finding across three hunters, Run 32 is the strongest clean-attempt yet.**
- **Run 32 — DIRTY, streak stays 0 (the deploy/ops surface AX opened held a second real bug).** With
  the code surface converged, Run 31's AX proved the deployment layer was the last un-audited surface
  and that it holds real, high-impact bugs — so Run 32 mined that vein: adversary-on-AX + a fresh sweep
  of the REST of the deploy/ops layer (ansible `runner.py` + `recorder_node` role, the release-artifact
  checksum contract, Dockerfiles, `docker-compose.yml`, all Helm templates/probes, nginx beyond AX).
  The fresh sweep found `AY` — the web pod's **liveness AND readiness** probes both target `/healthz`,
  which nginx proxies to the API, so any API outage or slow first-install migration crash-loops the
  perfectly-healthy web tier (and, via readiness, pulls `/api` out of the ingress just as the API
  recovers). Fixed with a self-contained `location = /nginx-health` and both probes (+ the Compose
  healthcheck) pointed at it; red→green via a config-invariant test. The adversary-on-AX confirmed the
  body-size fix is correct but surfaced `R32-PROXY-BUFFERING` — stock `proxy_request_buffering on`
  under the now-unlimited body size defeats the app's pre-buffer 413 and disk-spools large uploads on
  the web tier; catalogued SUSPECTED (bounded blast radius, and the clean fix is a slice that would open
  the 60s upstream-timeout twin + needs an API streaming redesign, so not a one-liner). It also ruled
  OUT the timeout candidates with verbatim nginx docs (`client_body_timeout` resets on activity;
  `proxy_max_temp_file_size` is response-only). Dirty (landed AY) → streak 0. **Signal: the code is
  converged; the deploy/ops layer, never scrutinized by the 31 code-focused runs, is now yielding one
  real bug per pass (AX then AY) — it needs its own dedicated sweep to converge before a clean run is
  reachable. Both AX and AY are k8s/reverse-proxy topology bugs invisible to the Compose smokes, which
  hit the API directly; the through-nginx and through-ingress paths were simply never exercised.**
- **Run 33 — DIRTY, streak stays 0 (the dedicated deploy convergence Run 32 called for).** Three
  hunters flushed the deploy vein: k8s pod-lifecycle (+ AY-adversary), the nginx request path, and Helm
  chart render (this hunter installed `helm 3.16.3` and ran `helm template`/`helm lint` live across all
  three secret backends). Two CONFIRMED fixes landed: `AZ` — the API Deployment inherited RollingUpdate
  over a ReadWriteOnce PVC, so an image-tag upgrade deadlocks (new pod stuck Multi-Attach, `maxUnavailable`
  0 at replicas:1) → set `Recreate`; and `BA` — the documented externalSecrets/sealed recipe left the
  bundled Postgres on with an empty password while the API's `DATABASE_URL` carried the real one, so the
  controller never connects (docs/config fix, `helm template`-verified). The **nginx request path came
  back CLEAN** (SSE is demo-only, downloads are complete bodies unaffected by buffering, `/metrics` is
  RBAC-gated, OIDC/cookie building is env- not header-driven). Four items catalogued rather than fixed
  because each is a genuine slice, not a one-liner: `R33-API-READINESS` (the AY-twin — API readiness on
  static `/healthz`; a correct `/readyz` needs bounded DB-ping machinery that doesn't exist yet, and its
  benefit is conditioned on `replicaCount>1` which the RWO design precludes), `R33-MIGRATE-JOB` (the opt-in
  migration Job double-migrates + has no ordering hook + no DB-wait; the fixes are coupled and
  bundled-vs-external-DB-dependent, and the DEFAULT init-container path is already correct),
  `R33-HELM-RENDER-GATE` (no gate renders the chart — the cross-object bugs `BA`/`R33-MIGRATE-JOB` are
  invisible to text-only template tests), and `R33-WEB-CACHE` (index.html cache / immutable assets / gzip
  hardening). Dirty (landed AZ, BA) → streak 0. **Signal: the deploy convergence is working — three
  hunters over the whole deploy surface yielded 2 fixes + 4 catalogued slices and one CLEAN dimension
  (nginx). The remaining deploy risk is now concentrated in the catalogued slices (readiness `/readyz`,
  the migration-Job design, a helm render gate); once those are addressed as their own considered work,
  the deploy layer should stop yielding per-pass bugs and a clean run becomes reachable.**
- **Run 34 — CLEAN, streak 1 (FIRST clean run of the resumed series).** A broad clean-attempt across three
  hunters — adversary-on-Run-33-fixes (AZ/BA), a cross-surface completeness critic, and an agent↔controller
  JSON-contract sweep — that spanned an account-usage-limit interruption (the completeness + contract passes
  were cut off mid-run and resumed after the reset). All three came back with **zero confirmed findings**:
  (1) `AZ` (Recreate) is correct — the brief single-writer cutover blip is inherent/acceptable, PVC
  detach/attach ordering is safe, the bundled-Postgres StatefulSet correctly does NOT need the same fix
  (delete-then-create per ordinal releases the RWO PVC), and the web Deployment mounts no PVC; `BA`'s recipes
  are helm-render-verified correct (external-DB renders no bundled Postgres, sealed covered,
  `postgres.auth.existingSecret` mitigation works). (2) The completeness critic verified 13 cross-cutting
  modalities (agent→controller field/units contract, Prometheus/CSV-injection, upload integrity + the
  documented CAS residual, the code↔chart single-writer boundary, DST engine, auth-session lifecycle, RBAC
  DENY-precedence, agent crash-recovery, inventory reconcile, pagination bounds, secret non-leakage, HTTP
  timeouts) — every soft spot already carries an in-code comment naming the prior fix, nothing new. (3) The
  JSON-contract sweep traced all 8 agent↔controller payloads (inventory, heartbeat, health events, meter
  frames, recording job, cache-file upload, monitor chunks, bootstrap) end-to-end across Rust serde ↔ Zod ↔
  Drizzle and found the three layers agree (channel indices 1-based both sides, dBFS/quality units aligned,
  omitted-optionals reconciled, no field-drop). Only low leads: `R34-ORPHAN-POSTGRES` (catalogued) + a
  render-test gap that folds into `R33-HELM-RENDER-GATE`. Gates green (API 570 pass / 13 skip, tsc, oxlint,
  oxfmt, LOC), `main` current, zero product-file changes. Clean → **streak 1**. **The code surface converged
  at Run 31 and the deploy surface converged across Runs 31–33 (AX/AY/AZ/BA fixed, nginx clean); Run 34 is the
  first run where a full cross-surface sweep found nothing new to fix — the milestone the audit has been
  driving toward. Runs 35–38 continue the streak toward 5.**
- **Run 35 — DIRTY, streak reset to 0 (a fresh angle surfaced a real operator-facing bug).** Two hunters
  over the two least-audited surfaces: web-console RUNTIME correctness (cache-invalidation, optimistic
  races, pagination, auth/token lifecycle, formatters) and controller RESOURCE-LIFECYCLE (DB pools, runner
  re-entrancy, handle/stream cleanup, error-swallow). The resource-lifecycle pass came back **CLEAN** — 20
  memoized store pools (60 connections < 100 on the single supported replica), all six interval runners
  carry an in-flight `running` guard, file/stream handles close on every path, no silent write-loss beyond
  the deliberate best-effort paths. The web pass found `BB` — the health summary tiles counted only the
  current page, so they undercounted and contradicted the header total on the incident-triage screen
  (fixed by mirroring the jobs server-summary pattern, red→green). It also flagged the absence of a React
  error boundary (`R35-ERROR-BOUNDARY`, catalogued SUSPECTED — no confirmed reproducing throw). Dirty
  (landed BB) → streak 0. **Reaching streak 1 then finding a genuine bug on the very next fresh angle is
  the audit working as intended: the web-console runtime + resource-lifecycle dimensions had never been
  swept as a unit, and the summary-undercount is exactly the kind of long-tail, single-screen wrong-value
  that only a fresh lens surfaces. The cache-invalidation/auth/pagination surfaces were confirmed sound, so
  this dimension is now largely swept; the streak restarts from a more converged base.**
- **Run 36 — CLEAN, streak 1.** Generalized `BB` following the audit's discipline (as `AH`→Run 24, `AF`→Run 22):
  a CLASS sweep for "an operator-facing aggregate (tile/count/badge/total) computed over the current
  paginated page instead of the full filtered set", plus an adversary pass on the `BB` fix. **Both came
  back with no new confirmed bug.** The class sweep enumerated every page's aggregate and how it's
  computed and found the class has exactly TWO instances — jobs (G74) and health (BB) — both fixed; every
  other page either shows no summary tile (nodes/schedules/audit/access), sources its aggregate from a
  dedicated server endpoint (`/status`, `/recordings/facets`, calendar `meta`, runner `lastSummary`), or
  fetches full/bounded sets (recordings facets, settings, switchers, pickers) — the class is closed. The
  adversary confirmed `BB` itself is correct (summary===meta.total in both branches, activeCritical
  semantics preserved, safe fallback, no CSV twin) and surfaced one PRE-EXISTING low item
  (`R36-ADMIN-DENY-COUNT`, admin-under-DENY header/tile over-count — catalogued, needs a product call). A
  latent upload-queue `meta`-discarded note was judged the benign 200-cap picker pattern (G76), not a
  `BB` bug. Zero product-file changes, gates green, `main` current → clean, **streak 1**. The BB class is
  now COVERAGE-complete; Runs 37–40 continue toward 5.**
- **Run 37 — DIRTY, streak reset to 0.** Two fresh, less-recently-swept angles: the Rust agent's
  capture/job/cache STATE MACHINE and the scheduler's RECURRENCE/timezone math. The scheduler pass found
  `BC` — the `AT` every-N parity class had a residual on the **move-occurrence write path**: recomputing
  the skipped series' `nextRunAt` re-anchored parity to `now`, so moving one occurrence of a bi-weekly /
  every-N series silently shifted every future occurrence to the wrong slot ~half the time (fixed by
  passing the schedule's phase as `parityAnchor`; red→green via a `mock.timers`-pinned off-parity `now`).
  The Rust pass verified the state machine largely sound (six-status terminal detection, duration=0 bail,
  upload→complete honesty, eviction-only-after-upload, restart stitch-or-fail) but re-surfaced prior-audit
  **G61**: with `maxConcurrentRecordings ≥ 2` the concurrent capture workers share ONE `agent_state_file`,
  so a crash recovers only the last writer's job. That is a KNOWN, prior-catalogued, config-gated item
  (default `max_concurrent_recordings = 1`), so it's not a NEW finding — carried forward as
  `R37-CONCURRENT-STATE` (promoted to Medium given the controller-driven capacity reachability) with the
  fix deferred as a per-job-state concurrency refactor. Dirty (landed BC) → streak 0. **The scheduler
  every-N class is a recurring seam (AT fixed the read path at Run 28; BC the write path now) — with both
  the calendar-window and move-occurrence anchors corrected, that class is closed. The move-occurrence
  parity residual is exactly the kind of interval>1-only bug that interval:1 tests never exercise — a
  reminder to test the every-N path explicitly.**
- **Run 38 — DIRTY, streak stays 0.** Adversary-on-BC (all recurrence modes + case (b) + DST) +
  a fresh auth/OIDC/crypto SECURITY pass. Adversary confirmed `BC` correct everywhere and closed its
  last residual with `BD` — a null-`nextRunAt` recurring series would let the parity anchor fall back to
  `now` (fixed by anchoring to the dragged occurrence's phase; red→green). **With the calendar-window
  (AT), move-occurrence (BC), and null-anchor (BD) paths all corrected, the every-N parity class is fully
  closed.** The security pass verified the crypto/token/session/OIDC-flow layer HARDENED (AES-256-GCM with
  fresh per-op IVs + tag verification, timing-safe token/password comparison with a decoy-hash enumeration
  guard, hashed session lookup + TTL/revocation, OIDC state/nonce/PKCE/signature via openid-client,
  same-origin returnTo allowlist) but found `R38-OIDC-EMAIL-LINK` — a CONFIRMED weakness: OIDC links
  accounts by **email** (no `email_verified`, subject captured-but-unused), which can merge onto the
  fixed-UUID local-admin row and inherit **owner**. Reachability is IdP-config-contingent (the default
  `admin@rakkr.local` sentinel is safe on a pinned single-tenant Azure; it bites on multi-tenant /
  self-service / unverified-email IdPs or a real admin-email). **Catalogued, not unilaterally fixed** —
  the correct fix (require `email_verified` + link on subject via a schema migration + refuse binding to
  the local-admin id) is security-sensitive auth behavior needing a maintainer/product call; surfaced
  prominently. A new CONFIRMED finding makes the run dirty regardless → streak 0. **This is the headline
  security finding of the audit — the OIDC identity-linking key (email vs verified-subject) had never been
  examined by the flow-focused OIDC sweeps (Runs 8/27).**
- **Post-Run-38 — operator-directed fix `BE` (R38-OIDC-EMAIL-LINK resolved).** The operator chose to fix the
  OIDC finding rather than defer it. Landed `BE`: a `users.external_id` schema migration (`0044`) + a rewrite
  of the OIDC linking to key on the **IdP subject**, adopt legacy email-linked rows, and **refuse**
  (`oidc_email_conflict` → 403) any login whose email is owned by a different identity (closing the
  local-admin takeover), plus an `email_verified:false` reject. Red→green via `oidc-user-linking.test.ts`
  (incl. a DB-gated takeover proof — all 6 tests fail on the pre-fix email-merge). Note the intended
  behavior change: a federated login whose email equals the local-admin (or any existing) email is now
  REFUSED — the secure path to grant a federated user admin is an explicit role / OIDC group mapping under
  their own subject, not email-collision with the break-glass account. Gates green (API 576 + 3 DB-gated
  linking tests, `db:verify` replays `0044`, OIDC baseline re-verified). This landed changes, so the audit
  streak restarts from 0 on the next fresh run.**
- **Run 39 — CLEAN, streak 1.** Adversary-on-BE (the OIDC security fix) + a fresh convergence pass over the
  two least-swept subsystems (switcher + upload executor, plus metrics). **Both came back with no confirmed
  bug.** The adversary verified BE is SOUND — the linking key is the openid-client-validated id_token
  subject (not user-settable), `email_verified` fails closed on any non-canonical value, the legacy-adopt
  path can't be abused (operator-created users are always `provider:local`), the unique index + subject-
  derived update block hijack, migration `0044` is NULL-safe on a populated table, and legitimate
  re-login/email-change flows are preserved; its only leads were low diagnostics gaps (`R39-OIDC-RACE-502`,
  catalogued — an opaque 502 on a rare insert race / benign email-swap). The fresh pass traced the switcher
  driver/reconcile (no command injection, no non-owned-output route, enforce-only-when-live, no password
  leak, health-events-once) and the upload executor (partial-vs-uploaded labeling, cache-delete gated on
  all-confirmed, path-escape neutralized, atomic CAS claim, checksum verify) all clean, metrics bounded.
  Zero product-file changes, gates green, `main` current → clean, **streak 1**. The two headline recent
  areas (OIDC security, scheduler parity) are now both verified closed; Runs 40–43 continue toward 5.**
- **Run 40 — DIRTY, streak reset to 0.** Two fresh angles: a test-coverage completeness critic (untested
  branchy helpers) and the recorder-agent remainder (enhance/bootstrap/ALSA device-selection). The coverage
  critic found `BF` — `escapeLabel` (metrics) escaped `\n`/`\"`/`\\` but not `\r`, so a raw CR in an
  operator-set alias/room breaks the whole `/metrics` scrape (fixed red→green; a strict completeness gap in
  the escaping function). The recorder pass verified the crate sound in the high-risk areas (enhance buffer
  sizing hop-exact, 48 kHz guard, ffmpeg args zod-bounded, bootstrap key-wipe on every path incl. panic,
  ALSA device re-resolution + stitch-or-fail) but found `R40-ENHANCE-TAIL` — the enhanced rendition clips
  ~20 ms of tail + leads the raw by ~20 ms (DeepFilterNet streaming lookahead never flushed). It's Low (the
  RAW master is always preserved; only the denoised convenience copy has boundary artifacts) and CONFIRMED,
  but its safe fix is a DSP change that genuinely needs Linux/engine + real-audio verification (an
  audio-quality change deserves rig validation), so it's **catalogued with the fix recipe rather than
  landed on this Windows env**. Dirty (landed BF; a new confirmed finding) → streak 0. **The long tail is
  now down to a metrics-escaping completeness gap and a ~20 ms DSP boundary nit on a convenience rendition
  — the core surfaces (auth, scheduler, deploy, web, upload, switcher, rust state machine) are all verified
  converged. The remaining finds are increasingly peripheral.**
